// Expert Anvil (C++ track) batch 1: all 9 empty topics (finishes the track).
// Matches cpp_stl precedent: 8/topic (reorder,reorder,fix,fix,output,output,build,build),
// buggy_code/snippet_code/starter_code/answer_bank fields. No C++ compiler available here
// (same caveat as Java/C#) — low-risk standard syntax only, brace-balance checked.
// Tooling/non-deterministic topics (cpp_compilation_process, cpp_game_engines,
// cpp_undefined_behavior, cpp_build_systems) use cout narration.
const fs = require("fs");
const path = require("path");
const KB_PATH = path.join(__dirname, "..", "data", "knowledge_base.json");
const kb = JSON.parse(fs.readFileSync(KB_PATH, "utf8"));
const cpp = kb.tiers.find((t) => t.id === "expert").language_tracks.cpp;

function w(body) {
  return `#include <iostream>\nusing namespace std;\n\nint main() {\n${body}\n    return 0;\n}`;
}

const CONTENT = {
  cpp_manual_memory: [
    {
      id: "cpp_manual_memory_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program that allocates and frees a single int on the heap.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    int* p = new int(5);\n    cout << *p << endl;", "    delete p;\n    return 0;\n}"],
      solution_code: w("    int* p = new int(5);\n    cout << *p << endl;\n    delete p;"),
      expected_output: "5",
      hints: ["`new` allocates before the value can be dereferenced.", "`delete` must come after the pointer is done being used."],
      solution_summary: "new int(5) allocates on the heap, *p dereferences to print 5, then delete frees the memory.",
      key_concepts: ["manual memory", "new", "delete"],
      answer_bank: [w("    int* p = new int(5);\n    cout << *p << endl;\n    delete p;")],
    },
    {
      id: "cpp_manual_memory_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program allocating and freeing a heap array.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    int* arr = new int[3]{1,2,3};\n    cout << arr[0] + arr[1] + arr[2] << endl;", "    delete[] arr;\n    return 0;\n}"],
      solution_code: w("    int* arr = new int[3]{1,2,3};\n    cout << arr[0] + arr[1] + arr[2] << endl;\n    delete[] arr;"),
      expected_output: "6",
      hints: ["Array allocation with `new[]` must be freed with `delete[]`, not plain `delete`."],
      solution_summary: "new int[3]{1,2,3} allocates the array; 1+2+3=6 is printed, then delete[] frees it correctly.",
      key_concepts: ["manual memory", "new[]", "delete[]"],
      answer_bank: [w("    int* arr = new int[3]{1,2,3};\n    cout << arr[0] + arr[1] + arr[2] << endl;\n    delete[] arr;")],
    },
    {
      id: "cpp_manual_memory_wc3", type: "fix",
      prompt: "This program leaks memory because it never frees the allocated int. Fix it.",
      buggy_code: w("    int* p = new int(10);\n    cout << *p << endl;"),
      solution_code: w("    int* p = new int(10);\n    cout << *p << endl;\n    delete p;"),
      expected_output: "10",
      hints: ["Every `new` needs a matching `delete` once the memory is no longer needed."],
      solution_summary: "Adding `delete p;` after use frees the allocated memory, preventing the leak.",
      key_concepts: ["manual memory", "memory leak"],
      answer_bank: [w("    int* p = new int(10);\n    cout << *p << endl;\n    delete p;")],
    },
    {
      id: "cpp_manual_memory_wc4", type: "fix",
      prompt: "This program uses the wrong delete form for an array allocation. Fix it.",
      buggy_code: w("    int* arr = new int[2]{7,8};\n    cout << arr[0] << endl;\n    delete arr;"),
      solution_code: w("    int* arr = new int[2]{7,8};\n    cout << arr[0] << endl;\n    delete[] arr;"),
      expected_output: "7",
      hints: ["Array allocations (`new[]`) require `delete[]`, not plain `delete`, or behavior is undefined."],
      solution_summary: "Changing `delete arr` to `delete[] arr` correctly matches the `new int[2]` array allocation.",
      key_concepts: ["manual memory", "delete[]"],
      answer_bank: [w("    int* arr = new int[2]{7,8};\n    cout << arr[0] << endl;\n    delete[] arr;")],
    },
    {
      id: "cpp_manual_memory_wc5", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: w("    int* p = new int(3);\n    *p = *p + 4;\n    cout << *p << endl;\n    delete p;"),
      solution_code: w("    int* p = new int(3);\n    *p = *p + 4;\n    cout << *p << endl;\n    delete p;"),
      expected_output: "7",
      hints: ["*p starts at 3; *p = *p + 4 updates it to 7."],
      solution_summary: "The heap int starts at 3, is updated to 3+4=7, then printed and freed.",
      key_concepts: ["manual memory", "pointer dereference"], answer_bank: [],
    },
    {
      id: "cpp_manual_memory_wc6", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: w("    int* arr = new int[3]{10,20,30};\n    cout << arr[1] << endl;\n    delete[] arr;"),
      solution_code: w("    int* arr = new int[3]{10,20,30};\n    cout << arr[1] << endl;\n    delete[] arr;"),
      expected_output: "20",
      hints: ["arr[1] is the second element, 20."],
      solution_summary: "arr[1] accesses the second heap array element, 20.",
      key_concepts: ["manual memory", "heap array"], answer_bank: [],
    },
    {
      id: "cpp_manual_memory_wc7", type: "build",
      prompt: "Write a program that allocates a heap double set to 2.5, prints it, then frees it.",
      starter_code: w("    // allocate, print, and free here"),
      solution_code: w("    double* p = new double(2.5);\n    cout << *p << endl;\n    delete p;"),
      expected_output: "2.5",
      hints: ["Use `new double(2.5)` then dereference with `*p`."],
      solution_summary: "new double(2.5) allocates the value, *p prints 2.5, then delete frees it.",
      key_concepts: ["manual memory"],
      answer_bank: [w("    double* p = new double(2.5);\n    cout << *p << endl;\n    delete p;")],
    },
    {
      id: "cpp_manual_memory_wc8", type: "build",
      prompt: "Write a program allocating a heap array of 4 ints {1,2,3,4}, printing their sum, then freeing the array correctly.",
      starter_code: w("    // allocate the array, sum it, print, and free here"),
      solution_code: w("    int* arr = new int[4]{1,2,3,4};\n    int sum = 0;\n    for (int i = 0; i < 4; i++) sum += arr[i];\n    cout << sum << endl;\n    delete[] arr;"),
      expected_output: "10",
      hints: ["1+2+3+4=10.", "Use `delete[]` since this is an array allocation."],
      solution_summary: "The loop sums the 4 heap elements to 10, then delete[] frees the array.",
      key_concepts: ["manual memory", "delete[]"],
      answer_bank: [w("    int* arr = new int[4]{1,2,3,4};\n    int sum = 0;\n    for (int i = 0; i < 4; i++) sum += arr[i];\n    cout << sum << endl;\n    delete[] arr;")],
    },
  ],
  cpp_pointers_references_practical: [
    {
      id: "cpp_pointers_references_practical_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program using a reference to modify a variable.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nvoid increment(int& n) {\n    n = n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(x);", "    cout << x << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid increment(int& n) {\n    n = n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "6",
      hints: ["increment must be defined before main uses it.", "n is a reference, so modifying n modifies x directly."],
      solution_summary: "increment(x) modifies x through the reference parameter n, changing x from 5 to 6.",
      key_concepts: ["reference", "pass by reference"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid increment(int& n) {\n    n = n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(x);\n    cout << x << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_pointers_references_practical_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program using a pointer to modify a variable.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nvoid increment(int* n) {\n    *n = *n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(&x);", "    cout << x << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid increment(int* n) {\n    *n = *n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(&x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "6",
      hints: ["`&x` passes x's address; `*n` dereferences the pointer to reach x's value."],
      solution_summary: "increment(&x) passes x's address; *n = *n + 1 modifies x through the pointer, changing it to 6.",
      key_concepts: ["pointer", "address-of operator", "dereference"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid increment(int* n) {\n    *n = *n + 1;\n}\n\nint main() {\n    int x = 5;\n    increment(&x);\n    cout << x << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_pointers_references_practical_wc3", type: "fix",
      prompt: "This program fails to compile because it dereferences x directly instead of taking its address for the pointer parameter. Fix it.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nvoid setTen(int* n) {\n    *n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(x);\n    cout << x << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid setTen(int* n) {\n    *n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(&x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "10",
      hints: ["setTen expects an int*, so pass &x, not x itself."],
      solution_summary: "Passing &x (x's address) matches setTen's int* parameter, letting it set x to 10.",
      key_concepts: ["pointer", "address-of operator", "compile error"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid setTen(int* n) {\n    *n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(&x);\n    cout << x << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_pointers_references_practical_wc4", type: "fix",
      prompt: "This program silently doesn't modify x because the parameter is a plain int (pass-by-value), not a reference. Fix it to actually modify x.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nvoid setTen(int n) {\n    n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(x);\n    cout << x << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid setTen(int& n) {\n    n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "10",
      hints: ["Pass-by-value copies x, so changes inside setTen don't affect the original.", "Changing the parameter to `int& n` makes it a reference, modifying x directly."],
      solution_summary: "Changing the parameter to a reference (int& n) lets setTen actually modify x, changing it to 10.",
      key_concepts: ["reference", "pass by value vs reference"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid setTen(int& n) {\n    n = 10;\n}\n\nint main() {\n    int x = 5;\n    setTen(x);\n    cout << x << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_pointers_references_practical_wc5", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: w("    int x = 7;\n    int* p = &x;\n    cout << *p << endl;"),
      solution_code: w("    int x = 7;\n    int* p = &x;\n    cout << *p << endl;"),
      expected_output: "7",
      hints: ["p holds x's address; *p dereferences back to x's value, 7."],
      solution_summary: "p points to x, and *p dereferences it to print x's value, 7.",
      key_concepts: ["pointer", "dereference"], answer_bank: [],
    },
    {
      id: "cpp_pointers_references_practical_wc6", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: w("    int x = 3;\n    int& ref = x;\n    ref = 9;\n    cout << x << endl;"),
      solution_code: w("    int x = 3;\n    int& ref = x;\n    ref = 9;\n    cout << x << endl;"),
      expected_output: "9",
      hints: ["ref is a reference to x, so assigning to ref assigns to x itself."],
      solution_summary: "ref is bound to x; assigning ref = 9 changes x to 9 directly.",
      key_concepts: ["reference"], answer_bank: [],
    },
    {
      id: "cpp_pointers_references_practical_wc7", type: "build",
      prompt: "Write a function `void doubleIt(int& n)` that doubles n via reference, call it on x=6 in main, and print x.",
      starter_code: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int& n) {\n    // implement here\n}\n\nint main() {\n    // call doubleIt and print x here\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int& n) {\n    n = n * 2;\n}\n\nint main() {\n    int x = 6;\n    doubleIt(x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "12",
      hints: ["n = n * 2 modifies the referenced variable directly."],
      solution_summary: "doubleIt(x) doubles x through the reference, changing it from 6 to 12.",
      key_concepts: ["reference"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int& n) {\n    n = n * 2;\n}\n\nint main() {\n    int x = 6;\n    doubleIt(x);\n    cout << x << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_pointers_references_practical_wc8", type: "build",
      prompt: "Write a function `void doubleIt(int* n)` (pointer version) that doubles the pointed-to value, call it on x=6 with &x, and print x.",
      starter_code: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int* n) {\n    // implement here\n}\n\nint main() {\n    // call doubleIt and print x here\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int* n) {\n    *n = *n * 2;\n}\n\nint main() {\n    int x = 6;\n    doubleIt(&x);\n    cout << x << endl;\n    return 0;\n}",
      expected_output: "12",
      hints: ["*n = *n * 2 dereferences to modify the pointed-to value.", "Pass &x since doubleIt expects an int*."],
      solution_summary: "doubleIt(&x) doubles x through the pointer, changing it from 6 to 12.",
      key_concepts: ["pointer", "dereference"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int* n) {\n    *n = *n * 2;\n}\n\nint main() {\n    int x = 6;\n    doubleIt(&x);\n    cout << x << endl;\n    return 0;\n}"],
    },
  ],
  cpp_raii: [
    {
      id: "cpp_raii_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program where a class's destructor runs automatically when it goes out of scope.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;", "    return 0;\n}"],
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}",
      expected_output: "acquired\nusing\nreleased",
      hints: ["The constructor runs when r is created; the destructor runs automatically when main ends and r goes out of scope."],
      solution_summary: "Resource's constructor prints 'acquired', 'using' prints next, then the destructor automatically prints 'released' as r goes out of scope.",
      key_concepts: ["RAII", "constructor", "destructor"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_raii_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program where a resource is released at the end of an inner scope block, before main ends.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    {\n        Resource r;\n        cout << \"using\" << endl;\n    }", "    cout << \"after scope\" << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    {\n        Resource r;\n        cout << \"using\" << endl;\n    }\n    cout << \"after scope\" << endl;\n    return 0;\n}",
      expected_output: "acquired\nusing\nreleased\nafter scope",
      hints: ["r's destructor fires exactly when the inner braces close, before 'after scope' prints."],
      solution_summary: "r's destructor runs the moment the inner block ends, printing 'released' before 'after scope' runs.",
      key_concepts: ["RAII", "scope"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    {\n        Resource r;\n        cout << \"using\" << endl;\n    }\n    cout << \"after scope\" << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_raii_wc3", type: "fix",
      prompt: "This class fails to release its resource because it's missing a destructor entirely. Fix it.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}",
      expected_output: "acquired\nusing\nreleased",
      hints: ["RAII requires a destructor (`~Resource()`) to run cleanup automatically."],
      solution_summary: "Adding a destructor lets Resource automatically release its resource when it goes out of scope.",
      key_concepts: ["RAII", "destructor"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_raii_wc4", type: "fix",
      prompt: "This program's destructor never fires because the object is created with `new` but never destroyed. Fix it to use a stack-allocated object instead (or explicitly delete it).",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource* r = new Resource();\n    cout << \"using\" << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}",
      expected_output: "acquired\nusing\nreleased",
      hints: ["A heap-allocated object (`new`) needs an explicit `delete` to trigger its destructor.", "Using a stack object (`Resource r;`) lets RAII destroy it automatically when it goes out of scope."],
      solution_summary: "Switching to a stack-allocated Resource r lets its destructor run automatically via RAII, instead of leaking a `new`'d object that's never deleted.",
      key_concepts: ["RAII", "stack vs heap"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << \"acquired\" << endl; }\n    ~Resource() { cout << \"released\" << endl; }\n};\n\nint main() {\n    Resource r;\n    cout << \"using\" << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_raii_wc5", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\nusing namespace std;\n\nclass Logger {\npublic:\n    Logger() { cout << \"open\" << endl; }\n    ~Logger() { cout << \"close\" << endl; }\n};\n\nint main() {\n    Logger a;\n    Logger b;\n    cout << \"work\" << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Logger {\npublic:\n    Logger() { cout << \"open\" << endl; }\n    ~Logger() { cout << \"close\" << endl; }\n};\n\nint main() {\n    Logger a;\n    Logger b;\n    cout << \"work\" << endl;\n    return 0;\n}",
      expected_output: "open\nopen\nwork\nclose\nclose",
      hints: ["Both constructors run first (a then b), then 'work', then destructors run in reverse order of construction (b then a)."],
      solution_summary: "a and b are constructed in order (open, open), 'work' prints, then destructors run in reverse construction order (close, close) — b's first, then a's.",
      key_concepts: ["RAII", "destructor order"], answer_bank: [],
    },
    {
      id: "cpp_raii_wc6", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\nusing namespace std;\n\nclass Guard {\npublic:\n    Guard() { cout << \"lock\" << endl; }\n    ~Guard() { cout << \"unlock\" << endl; }\n};\n\nvoid critical() {\n    Guard g;\n    cout << \"critical section\" << endl;\n}\n\nint main() {\n    critical();\n    cout << \"done\" << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass Guard {\npublic:\n    Guard() { cout << \"lock\" << endl; }\n    ~Guard() { cout << \"unlock\" << endl; }\n};\n\nvoid critical() {\n    Guard g;\n    cout << \"critical section\" << endl;\n}\n\nint main() {\n    critical();\n    cout << \"done\" << endl;\n    return 0;\n}",
      expected_output: "lock\ncritical section\nunlock\ndone",
      hints: ["g's destructor fires when critical() returns, before control returns to main."],
      solution_summary: "Guard locks, prints 'critical section', then unlocks automatically as critical() returns, before 'done' prints in main.",
      key_concepts: ["RAII", "function scope"], answer_bank: [],
    },
    {
      id: "cpp_raii_wc7", type: "build",
      prompt: "Write a class FileHandle whose constructor prints 'open file' and destructor prints 'close file', then create one in main between two other print statements: 'start' before, 'end' after.",
      starter_code: "#include <iostream>\nusing namespace std;\n\nclass FileHandle {\n    // add constructor/destructor here\n};\n\nint main() {\n    // print start, create FileHandle, print end\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass FileHandle {\npublic:\n    FileHandle() { cout << \"open file\" << endl; }\n    ~FileHandle() { cout << \"close file\" << endl; }\n};\n\nint main() {\n    cout << \"start\" << endl;\n    FileHandle f;\n    cout << \"end\" << endl;\n    return 0;\n}",
      expected_output: "start\nopen file\nend\nclose file",
      hints: ["The destructor fires automatically at the end of main, after 'end' prints."],
      solution_summary: "'start' prints, then the constructor opens the file, then 'end' prints, then the destructor closes it automatically as main ends.",
      key_concepts: ["RAII", "constructor", "destructor"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass FileHandle {\npublic:\n    FileHandle() { cout << \"open file\" << endl; }\n    ~FileHandle() { cout << \"close file\" << endl; }\n};\n\nint main() {\n    cout << \"start\" << endl;\n    FileHandle f;\n    cout << \"end\" << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_raii_wc8", type: "build",
      prompt: "Write a class ScopedTimer whose constructor prints 'timer started' and destructor prints 'timer stopped', create one inside an inner scope block within main, and print 'after block' after the block ends.",
      starter_code: "#include <iostream>\nusing namespace std;\n\nclass ScopedTimer {\n    // add constructor/destructor here\n};\n\nint main() {\n    // create ScopedTimer inside an inner { } block, then print after it\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\nclass ScopedTimer {\npublic:\n    ScopedTimer() { cout << \"timer started\" << endl; }\n    ~ScopedTimer() { cout << \"timer stopped\" << endl; }\n};\n\nint main() {\n    {\n        ScopedTimer t;\n    }\n    cout << \"after block\" << endl;\n    return 0;\n}",
      expected_output: "timer started\ntimer stopped\nafter block",
      hints: ["The destructor fires the instant the inner block's closing brace is reached, before 'after block' prints."],
      solution_summary: "ScopedTimer's constructor and destructor both fire within the inner block, both completing before 'after block' prints.",
      key_concepts: ["RAII", "scope"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\nclass ScopedTimer {\npublic:\n    ScopedTimer() { cout << \"timer started\" << endl; }\n    ~ScopedTimer() { cout << \"timer stopped\" << endl; }\n};\n\nint main() {\n    {\n        ScopedTimer t;\n    }\n    cout << \"after block\" << endl;\n    return 0;\n}"],
    },
  ],
  cpp_templates: [
    {
      id: "cpp_templates_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program using a function template with an int.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {", "    cout << maxOf(3, 7) << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << maxOf(3, 7) << endl;\n    return 0;\n}",
      expected_output: "7",
      hints: ["The template must be defined before main calls it.", "T is deduced as int from the arguments 3 and 7."],
      solution_summary: "maxOf<int>(3, 7) is instantiated from the template, returning 7 since 7 > 3.",
      key_concepts: ["template", "type deduction"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << maxOf(3, 7) << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_templates_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program using the same template with strings, showing it works for multiple types.",
      shuffled_lines: ["#include <iostream>\n#include <string>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {", "    cout << maxOf(string(\"apple\"), string(\"banana\")) << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\n#include <string>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << maxOf(string(\"apple\"), string(\"banana\")) << endl;\n    return 0;\n}",
      expected_output: "banana",
      hints: ["T is deduced as string this time.", "\"banana\" > \"apple\" alphabetically, so it's returned."],
      solution_summary: "maxOf<string> is instantiated with strings; \"banana\" > \"apple\" alphabetically, so \"banana\" is returned.",
      key_concepts: ["template", "generic programming"],
      answer_bank: ["#include <iostream>\n#include <string>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << maxOf(string(\"apple\"), string(\"banana\")) << endl;\n    return 0;\n}",],
    },
    {
      id: "cpp_templates_wc3", type: "fix",
      prompt: "This program fails to compile because it duplicates a function for int and double instead of using a template. Fix it with a single template function.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nint maxOf(int a, int b) { return a > b ? a : b; }\ndouble maxOf(double a, double b) { return a > b ? a : b; }\n\nint main() {\n    cout << maxOf(3, 7) << endl;\n    cout << maxOf(2.5, 1.5) << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) { return a > b ? a : b; }\n\nint main() {\n    cout << maxOf(3, 7) << endl;\n    cout << maxOf(2.5, 1.5) << endl;\n    return 0;\n}",
      expected_output: "7\n2.5",
      hints: ["A single template function replaces both duplicated overloads.", "This isn't actually a compile error in the original, but it demonstrates the redundancy templates eliminate."],
      solution_summary: "One template function replaces the two duplicated overloads, still correctly producing 7 and 2.5.",
      key_concepts: ["template", "code duplication"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT maxOf(T a, T b) { return a > b ? a : b; }\n\nint main() {\n    cout << maxOf(3, 7) << endl;\n    cout << maxOf(2.5, 1.5) << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_templates_wc4", type: "fix",
      prompt: "This template function fails to compile due to a missing `typename` keyword in its declaration. Fix it.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\ntemplate <T>\nT square(T x) {\n    return x * x;\n}\n\nint main() {\n    cout << square(4) << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT square(T x) {\n    return x * x;\n}\n\nint main() {\n    cout << square(4) << endl;\n    return 0;\n}",
      expected_output: "16",
      hints: ["Template parameters must be declared with `typename` (or `class`), like `template <typename T>`."],
      solution_summary: "Adding `typename` before T correctly declares the template parameter, and square(4) returns 16.",
      key_concepts: ["template", "typename"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT square(T x) {\n    return x * x;\n}\n\nint main() {\n    cout << square(4) << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_templates_wc5", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT triple(T x) {\n    return x * 3;\n}\n\nint main() {\n    cout << triple(4) << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT triple(T x) {\n    return x * 3;\n}\n\nint main() {\n    cout << triple(4) << endl;\n    return 0;\n}",
      expected_output: "12",
      hints: ["triple(4) computes 4 * 3."],
      solution_summary: "T is deduced as int; triple(4) returns 4*3=12.",
      key_concepts: ["template"], answer_bank: [],
    },
    {
      id: "cpp_templates_wc6", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT triple(T x) {\n    return x * 3;\n}\n\nint main() {\n    cout << triple(1.5) << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT triple(T x) {\n    return x * 3;\n}\n\nint main() {\n    cout << triple(1.5) << endl;\n    return 0;\n}",
      expected_output: "4.5",
      hints: ["T is deduced as double this time; 1.5 * 3 = 4.5."],
      solution_summary: "T is deduced as double; triple(1.5) returns 1.5*3=4.5.",
      key_concepts: ["template", "type deduction"], answer_bank: [],
    },
    {
      id: "cpp_templates_wc7", type: "build",
      prompt: "Write a template function `T sum3(T a, T b, T c)` returning a+b+c, and print sum3(1, 2, 3) and sum3(1.5, 2.5, 1.0).",
      starter_code: "#include <iostream>\nusing namespace std;\n\n// define the template here\n\nint main() {\n    // print both calls here\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT sum3(T a, T b, T c) {\n    return a + b + c;\n}\n\nint main() {\n    cout << sum3(1, 2, 3) << endl;\n    cout << sum3(1.5, 2.5, 1.0) << endl;\n    return 0;\n}",
      expected_output: "6\n5",
      hints: ["1+2+3=6; 1.5+2.5+1.0=5."],
      solution_summary: "sum3 is instantiated for both int (1+2+3=6) and double (1.5+2.5+1.0=5).",
      key_concepts: ["template"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT sum3(T a, T b, T c) {\n    return a + b + c;\n}\n\nint main() {\n    cout << sum3(1, 2, 3) << endl;\n    cout << sum3(1.5, 2.5, 1.0) << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_templates_wc8", type: "build",
      prompt: "Write a template function `bool isEqual(T a, T b)` returning a==b, and print isEqual(5, 5) and isEqual(3, 4).",
      starter_code: "#include <iostream>\nusing namespace std;\n\n// define the template here\n\nint main() {\n    // print both calls here\n    return 0;\n}",
      solution_code: "#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nbool isEqual(T a, T b) {\n    return a == b;\n}\n\nint main() {\n    cout << isEqual(5, 5) << endl;\n    cout << isEqual(3, 4) << endl;\n    return 0;\n}",
      expected_output: "1\n0",
      hints: ["cout prints bool as 1 (true) or 0 (false).", "5==5 is true; 3==4 is false."],
      solution_summary: "isEqual(5,5) is true (prints 1), isEqual(3,4) is false (prints 0), cout's default bool formatting.",
      key_concepts: ["template", "bool"],
      answer_bank: ["#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nbool isEqual(T a, T b) {\n    return a == b;\n}\n\nint main() {\n    cout << isEqual(5, 5) << endl;\n    cout << isEqual(3, 4) << endl;\n    return 0;\n}"],
    },
  ],
  cpp_smart_pointers: [
    {
      id: "cpp_smart_pointers_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program using unique_ptr to automatically manage memory.",
      shuffled_lines: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(5);", "    cout << *p << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(5);\n    cout << *p << endl;\n    return 0;\n}",
      expected_output: "5",
      hints: ["make_unique<int>(5) allocates and wraps the int in a unique_ptr.", "No manual delete is needed — it's freed automatically when p goes out of scope."],
      solution_summary: "unique_ptr manages the allocated int automatically; *p dereferences to print 5, freed automatically at scope end.",
      key_concepts: ["smart pointer", "unique_ptr"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(5);\n    cout << *p << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_smart_pointers_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program using shared_ptr with two owners.",
      shuffled_lines: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(10);\n    shared_ptr<int> b = a;", "    cout << *a + *b << endl;\n    return 0;\n}"],
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(10);\n    shared_ptr<int> b = a;\n    cout << *a + *b << endl;\n    return 0;\n}",
      expected_output: "20",
      hints: ["shared_ptr allows multiple owners of the same allocated memory.", "*a and *b both dereference to 10; 10+10=20."],
      solution_summary: "a and b share ownership of the same int (10); *a + *b is 10+10=20.",
      key_concepts: ["smart pointer", "shared_ptr"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(10);\n    shared_ptr<int> b = a;\n    cout << *a + *b << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_smart_pointers_wc3", type: "fix",
      prompt: "This program leaks memory using raw new/delete where a smart pointer would be safer. Fix it using unique_ptr.",
      buggy_code: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int* p = new int(8);\n    cout << *p << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(8);\n    cout << *p << endl;\n    return 0;\n}",
      expected_output: "8",
      hints: ["The raw `new int(8)` is never deleted, leaking memory.", "Replacing it with `make_unique<int>(8)` automatically frees the memory when p goes out of scope."],
      solution_summary: "Switching to unique_ptr via make_unique automatically manages and frees the memory, eliminating the leak.",
      key_concepts: ["smart pointer", "unique_ptr", "memory leak"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(8);\n    cout << *p << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_smart_pointers_wc4", type: "fix",
      prompt: "This program fails to compile because it tries to copy a unique_ptr, which isn't allowed. Fix it using shared_ptr instead, since two owners are actually needed here.",
      buggy_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> a = make_unique<int>(4);\n    unique_ptr<int> b = a;\n    cout << *a + *b << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(4);\n    shared_ptr<int> b = a;\n    cout << *a + *b << endl;\n    return 0;\n}",
      expected_output: "8",
      hints: ["unique_ptr can't be copied — only moved — since it enforces sole ownership.", "shared_ptr allows exactly this kind of shared, copyable ownership."],
      solution_summary: "Switching to shared_ptr allows b to legally share ownership with a, and *a + *b correctly computes 4+4=8.",
      key_concepts: ["smart pointer", "shared_ptr", "unique_ptr"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(4);\n    shared_ptr<int> b = a;\n    cout << *a + *b << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_smart_pointers_wc5", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(6);\n    *p = *p + 4;\n    cout << *p << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(6);\n    *p = *p + 4;\n    cout << *p << endl;\n    return 0;\n}",
      expected_output: "10",
      hints: ["*p starts at 6; *p = *p + 4 updates it to 10."],
      solution_summary: "The managed int starts at 6, updated to 6+4=10, then printed.",
      key_concepts: ["smart pointer", "unique_ptr"], answer_bank: [],
    },
    {
      id: "cpp_smart_pointers_wc6", type: "output",
      prompt: "Read this code carefully and type exactly what it will print.",
      snippet_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(3);\n    shared_ptr<int> b = a;\n    *b = 99;\n    cout << *a << endl;\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(3);\n    shared_ptr<int> b = a;\n    *b = 99;\n    cout << *a << endl;\n    return 0;\n}",
      expected_output: "99",
      hints: ["a and b share the same underlying int, so mutating through b also changes what a sees."],
      solution_summary: "Since a and b share the same object, mutating *b to 99 also changes *a to 99.",
      key_concepts: ["smart pointer", "shared_ptr"], answer_bank: [],
    },
    {
      id: "cpp_smart_pointers_wc7", type: "build",
      prompt: "Write a program using unique_ptr<int> to manage a value of 15, print it, then reassign it to a new managed value of 30 and print again.",
      starter_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    // create, print, reassign, print\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(15);\n    cout << *p << endl;\n    p = make_unique<int>(30);\n    cout << *p << endl;\n    return 0;\n}",
      expected_output: "15\n30",
      hints: ["Reassigning p to a new make_unique automatically frees the old managed value first."],
      solution_summary: "p first manages 15 (printed), then is reassigned to a new unique_ptr managing 30 (printed), with the old one freed automatically.",
      key_concepts: ["smart pointer", "unique_ptr"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    unique_ptr<int> p = make_unique<int>(15);\n    cout << *p << endl;\n    p = make_unique<int>(30);\n    cout << *p << endl;\n    return 0;\n}"],
    },
    {
      id: "cpp_smart_pointers_wc8", type: "build",
      prompt: "Write a program using shared_ptr<int> with two shared owners, mutate through one, and print the value through the other.",
      starter_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    // create two shared owners, mutate through one, print through the other\n    return 0;\n}",
      solution_code: "#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(1);\n    shared_ptr<int> b = a;\n    *a = 50;\n    cout << *b << endl;\n    return 0;\n}",
      expected_output: "50",
      hints: ["a and b share the same underlying object, so mutating through a is visible through b."],
      solution_summary: "a and b share ownership of the same int; mutating *a to 50 is visible through *b, printing 50.",
      key_concepts: ["smart pointer", "shared_ptr"],
      answer_bank: ["#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    shared_ptr<int> a = make_shared<int>(1);\n    shared_ptr<int> b = a;\n    *a = 50;\n    cout << *b << endl;\n    return 0;\n}"],
    },
  ],
  cpp_compilation_process: [
    {
      id: "cpp_compilation_process_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating the C++ compilation pipeline.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Step 1: the preprocessor expands #include and macros\" << endl;\n    cout << \"Step 2: the compiler translates source into object code\" << endl;", "    cout << \"Step 3: the linker combines object files into an executable\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"Step 1: the preprocessor expands #include and macros\" << endl;\n    cout << \"Step 2: the compiler translates source into object code\" << endl;\n    cout << \"Step 3: the linker combines object files into an executable\" << endl;"),
      expected_output: "Step 1: the preprocessor expands #include and macros\nStep 2: the compiler translates source into object code\nStep 3: the linker combines object files into an executable",
      hints: ["Preprocessing happens before compiling; compiling happens before linking."],
      solution_summary: "The pipeline runs preprocessing, then compiling to object code, then linking into a final executable, in that order.",
      key_concepts: ["compilation process", "linker"],
      answer_bank: [w("    cout << \"Step 1: the preprocessor expands #include and macros\" << endl;\n    cout << \"Step 2: the compiler translates source into object code\" << endl;\n    cout << \"Step 3: the linker combines object files into an executable\" << endl;")],
    },
    {
      id: "cpp_compilation_process_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating why a 'function not found' error can be a linker error, not a typo.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"A function declared but never defined compiles fine\" << endl;", "    cout << \"The linker fails later, unable to find that function's actual code\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"A function declared but never defined compiles fine\" << endl;\n    cout << \"The linker fails later, unable to find that function's actual code\" << endl;"),
      expected_output: "A function declared but never defined compiles fine\nThe linker fails later, unable to find that function's actual code",
      hints: ["Compiling succeeds first (it only checks declarations); linking fails afterward when the definition is missing."],
      solution_summary: "Compilation succeeds since only a declaration is needed, but linking fails afterward when it can't find the actual definition.",
      key_concepts: ["compilation process", "linker error"],
      answer_bank: [w("    cout << \"A function declared but never defined compiles fine\" << endl;\n    cout << \"The linker fails later, unable to find that function's actual code\" << endl;")],
    },
    {
      id: "cpp_compilation_process_wc3", type: "fix",
      prompt: "Fix this code so it accurately describes what the linker does.",
      buggy_code: w("    cout << \"The linker translates C++ syntax into machine instructions\" << endl;"),
      solution_code: w("    cout << \"The linker combines compiled object files into a single executable\" << endl;"),
      expected_output: "The linker combines compiled object files into a single executable",
      hints: ["Translating syntax to machine instructions is the compiler's job, not the linker's."],
      solution_summary: "The corrected statement accurately describes the linker as combining object files, not translating syntax.",
      key_concepts: ["linker", "compilation process"], answer_bank: [w("    cout << \"The linker combines compiled object files into a single executable\" << endl;")],
    },
    {
      id: "cpp_compilation_process_wc4", type: "fix",
      prompt: "Fix this code so it accurately describes what the preprocessor does.",
      buggy_code: w("    cout << \"The preprocessor executes the final compiled program\" << endl;"),
      solution_code: w("    cout << \"The preprocessor expands #include files and macros before real compilation begins\" << endl;"),
      expected_output: "The preprocessor expands #include files and macros before real compilation begins",
      hints: ["The preprocessor runs before compilation, not after — it never executes a final program."],
      solution_summary: "The corrected statement accurately describes the preprocessor's role expanding includes/macros before compilation.",
      key_concepts: ["preprocessor", "compilation process"], answer_bank: [w("    cout << \"The preprocessor expands #include files and macros before real compilation begins\" << endl;")],
    },
    {
      id: "cpp_compilation_process_wc5", type: "output",
      prompt: "What does this code print, simulating pipeline stages as data?",
      snippet_code: w("    string stages[3] = {\"preprocess\", \"compile\", \"link\"};\n    for (int i = 0; i < 3; i++) cout << stages[i] << endl;"),
      solution_code: w("    string stages[3] = {\"preprocess\", \"compile\", \"link\"};\n    for (int i = 0; i < 3; i++) cout << stages[i] << endl;"),
      expected_output: "preprocess\ncompile\nlink",
      hints: ["The loop prints each stage in array order."],
      solution_summary: "The loop iterates the stages array in order, printing each pipeline stage.",
      key_concepts: ["compilation process"], answer_bank: [],
    },
    {
      id: "cpp_compilation_process_wc6", type: "output",
      prompt: "What does this code print?",
      snippet_code: w("    bool linked = true;\n    cout << (linked ? \"executable ready\" : \"link error\") << endl;"),
      solution_code: w("    bool linked = true;\n    cout << (linked ? \"executable ready\" : \"link error\") << endl;"),
      expected_output: "executable ready",
      hints: ["linked is true, so the ternary picks the first option."],
      solution_summary: "Since linked is true, the ternary evaluates to \"executable ready\".",
      key_concepts: ["compilation process"], answer_bank: [],
    },
    {
      id: "cpp_compilation_process_wc7", type: "build",
      prompt: "Write a program storing pipeline stages (\"preprocess\", \"compile\", \"link\") in an array and printing each as 'stage: <name>'.",
      starter_code: w("    // declare the array and loop over it here"),
      solution_code: w("    string stages[3] = {\"preprocess\", \"compile\", \"link\"};\n    for (int i = 0; i < 3; i++) cout << \"stage: \" << stages[i] << endl;"),
      expected_output: "stage: preprocess\nstage: compile\nstage: link",
      hints: ["Loop through the array, formatting each stage into the message."],
      solution_summary: "The loop iterates stages in order, printing each formatted as 'stage: <name>'.",
      key_concepts: ["compilation process"],
      answer_bank: [w("    string stages[3] = {\"preprocess\", \"compile\", \"link\"};\n    for (int i = 0; i < 3; i++) cout << \"stage: \" << stages[i] << endl;")],
    },
    {
      id: "cpp_compilation_process_wc8", type: "build",
      prompt: "Write a program printing two facts, one per line: that compiling produces object files, and that linking combines them into an executable.",
      starter_code: w("    // print both facts here"),
      solution_code: w("    cout << \"Compiling produces object files from source code\" << endl;\n    cout << \"Linking combines object files into a final executable\" << endl;"),
      expected_output: "Compiling produces object files from source code\nLinking combines object files into a final executable",
      hints: ["Use two separate cout statements, in order."],
      solution_summary: "Two cout statements state, in order, what compiling produces and what linking does with it.",
      key_concepts: ["compilation process"],
      answer_bank: [w("    cout << \"Compiling produces object files from source code\" << endl;\n    cout << \"Linking combines object files into a final executable\" << endl;")],
    },
  ],
  cpp_game_engines: [
    {
      id: "cpp_game_engines_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating why C++ dominates game engines.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"C++ remains dominant in performance-critical software domains\" << endl;", "    cout << \"Game engines are among the clearest real-world examples of this\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"C++ remains dominant in performance-critical software domains\" << endl;\n    cout << \"Game engines are among the clearest real-world examples of this\" << endl;"),
      expected_output: "C++ remains dominant in performance-critical software domains\nGame engines are among the clearest real-world examples of this",
      hints: ["The general claim about performance-critical domains comes before the specific game-engine example."],
      solution_summary: "C++'s dominance in performance-critical software is stated first, then game engines as the clearest example.",
      key_concepts: ["game engines", "C++"],
      answer_bank: [w("    cout << \"C++ remains dominant in performance-critical software domains\" << endl;\n    cout << \"Game engines are among the clearest real-world examples of this\" << endl;")],
    },
    {
      id: "cpp_game_engines_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program naming Unreal Engine as a major example.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Unreal Engine is written primarily in C++\" << endl;", "    cout << \"It powers a huge share of modern AAA games\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"Unreal Engine is written primarily in C++\" << endl;\n    cout << \"It powers a huge share of modern AAA games\" << endl;"),
      expected_output: "Unreal Engine is written primarily in C++\nIt powers a huge share of modern AAA games",
      hints: ["What Unreal Engine is written in comes before the fact about its widespread use."],
      solution_summary: "Unreal Engine's C++ foundation is stated first, then its widespread use in AAA games.",
      key_concepts: ["game engines", "Unreal Engine"],
      answer_bank: [w("    cout << \"Unreal Engine is written primarily in C++\" << endl;\n    cout << \"It powers a huge share of modern AAA games\" << endl;")],
    },
    {
      id: "cpp_game_engines_wc3", type: "fix",
      prompt: "Fix this code so it accurately describes why C++ is used for game engines.",
      buggy_code: w("    cout << \"C++ is used for game engines because it's the easiest language to learn\" << endl;"),
      solution_code: w("    cout << \"C++ is used for game engines because of its raw performance and low-level control\" << endl;"),
      expected_output: "C++ is used for game engines because of its raw performance and low-level control",
      hints: ["C++'s appeal for game engines is performance and control, not ease of learning."],
      solution_summary: "The corrected statement accurately describes C++'s appeal for game engines as performance and control.",
      key_concepts: ["game engines", "C++"], answer_bank: [w("    cout << \"C++ is used for game engines because of its raw performance and low-level control\" << endl;")],
    },
    {
      id: "cpp_game_engines_wc4", type: "fix",
      prompt: "Fix this code so it accurately names a major C++ game engine.",
      buggy_code: w("    cout << \"Unreal Engine is written primarily in Python\" << endl;"),
      solution_code: w("    cout << \"Unreal Engine is written primarily in C++\" << endl;"),
      expected_output: "Unreal Engine is written primarily in C++",
      hints: ["Unreal Engine's core is C++, not Python."],
      solution_summary: "The corrected statement accurately names C++ as Unreal Engine's primary language.",
      key_concepts: ["game engines", "Unreal Engine"], answer_bank: [w("    cout << \"Unreal Engine is written primarily in C++\" << endl;")],
    },
    {
      id: "cpp_game_engines_wc5", type: "output",
      prompt: "What does this code print, simulating game engine facts as data?",
      snippet_code: w("    string facts[2] = {\"Unreal Engine\", \"performance-critical\"};\n    for (int i = 0; i < 2; i++) cout << facts[i] << endl;"),
      solution_code: w("    string facts[2] = {\"Unreal Engine\", \"performance-critical\"};\n    for (int i = 0; i < 2; i++) cout << facts[i] << endl;"),
      expected_output: "Unreal Engine\nperformance-critical",
      hints: ["The loop prints each element of facts in order."],
      solution_summary: "The loop iterates facts in order, printing each fact about C++ game engines.",
      key_concepts: ["game engines"], answer_bank: [],
    },
    {
      id: "cpp_game_engines_wc6", type: "output",
      prompt: "What does this code print?",
      snippet_code: w("    bool needsSpeed = true;\n    cout << (needsSpeed ? \"C++\" : \"scripting language\") << endl;"),
      solution_code: w("    bool needsSpeed = true;\n    cout << (needsSpeed ? \"C++\" : \"scripting language\") << endl;"),
      expected_output: "C++",
      hints: ["needsSpeed is true, so the ternary picks the first option."],
      solution_summary: "Since needsSpeed is true, the ternary evaluates to \"C++\".",
      key_concepts: ["game engines"], answer_bank: [],
    },
    {
      id: "cpp_game_engines_wc7", type: "build",
      prompt: "Write a program storing game engine facts (\"Unreal Engine\", \"raw performance\") in an array and printing each as 'game engines: <fact>'.",
      starter_code: w("        // declare the array and loop over it here"),
      solution_code: w("    string facts[2] = {\"Unreal Engine\", \"raw performance\"};\n    for (int i = 0; i < 2; i++) cout << \"game engines: \" << facts[i] << endl;"),
      expected_output: "game engines: Unreal Engine\ngame engines: raw performance",
      hints: ["Loop through the array, formatting each fact into the message."],
      solution_summary: "The loop iterates facts in order, printing each formatted as 'game engines: <fact>'.",
      key_concepts: ["game engines"],
      answer_bank: [w("    string facts[2] = {\"Unreal Engine\", \"raw performance\"};\n    for (int i = 0; i < 2; i++) cout << \"game engines: \" << facts[i] << endl;")],
    },
    {
      id: "cpp_game_engines_wc8", type: "build",
      prompt: "Write a program printing two facts, one per line: that C++ gives fine-grained control over memory, and that this matters for real-time game performance.",
      starter_code: w("    // print both facts here"),
      solution_code: w("    cout << \"C++ gives fine-grained control over memory\" << endl;\n    cout << \"This matters for real-time game performance\" << endl;"),
      expected_output: "C++ gives fine-grained control over memory\nThis matters for real-time game performance",
      hints: ["Use two separate cout statements, in order."],
      solution_summary: "Two cout statements state, in order, C++'s memory control and why it matters for real-time performance.",
      key_concepts: ["game engines"],
      answer_bank: [w("    cout << \"C++ gives fine-grained control over memory\" << endl;\n    cout << \"This matters for real-time game performance\" << endl;")],
    },
  ],
  cpp_undefined_behavior: [
    {
      id: "cpp_undefined_behavior_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating what undefined behavior means.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Code that compiles cleanly can still contain a genuine, serious bug\" << endl;", "    cout << \"Undefined behavior means the language spec places no guarantee on what happens\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"Code that compiles cleanly can still contain a genuine, serious bug\" << endl;\n    cout << \"Undefined behavior means the language spec places no guarantee on what happens\" << endl;"),
      expected_output: "Code that compiles cleanly can still contain a genuine, serious bug\nUndefined behavior means the language spec places no guarantee on what happens",
      hints: ["The surprising fact (clean compile, still buggy) comes before the technical definition."],
      solution_summary: "Clean-compiling but buggy code is described first, then defined as undefined behavior, where the spec makes no guarantee.",
      key_concepts: ["undefined behavior"],
      answer_bank: [w("    cout << \"Code that compiles cleanly can still contain a genuine, serious bug\" << endl;\n    cout << \"Undefined behavior means the language spec places no guarantee on what happens\" << endl;")],
    },
    {
      id: "cpp_undefined_behavior_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating why undefined behavior is dangerous.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"A bug from undefined behavior might work fine on one machine\" << endl;", "    cout << \"But fail unpredictably on a different machine, compiler, or build\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"A bug from undefined behavior might work fine on one machine\" << endl;\n    cout << \"But fail unpredictably on a different machine, compiler, or build\" << endl;"),
      expected_output: "A bug from undefined behavior might work fine on one machine\nBut fail unpredictably on a different machine, compiler, or build",
      hints: ["The false sense of safety (works fine here) comes before the actual danger (fails elsewhere)."],
      solution_summary: "Working fine on one machine is described first, then the danger of failing unpredictably elsewhere.",
      key_concepts: ["undefined behavior", "portability"],
      answer_bank: [w("    cout << \"A bug from undefined behavior might work fine on one machine\" << endl;\n    cout << \"But fail unpredictably on a different machine, compiler, or build\" << endl;")],
    },
    {
      id: "cpp_undefined_behavior_wc3", type: "fix",
      prompt: "Fix this code so it accurately describes what undefined behavior means.",
      buggy_code: w("    cout << \"Undefined behavior always causes an immediate, visible crash\" << endl;"),
      solution_code: w("    cout << \"Undefined behavior means the outcome is unpredictable, not guaranteed to crash visibly\" << endl;"),
      expected_output: "Undefined behavior means the outcome is unpredictable, not guaranteed to crash visibly",
      hints: ["Undefined behavior's danger is precisely that it's NOT guaranteed to crash — it might silently misbehave instead."],
      solution_summary: "The corrected statement accurately describes undefined behavior as unpredictable, not a guaranteed visible crash.",
      key_concepts: ["undefined behavior"], answer_bank: [w("    cout << \"Undefined behavior means the outcome is unpredictable, not guaranteed to crash visibly\" << endl;")],
    },
    {
      id: "cpp_undefined_behavior_wc4", type: "fix",
      prompt: "Fix this code so it accurately describes an example that can trigger undefined behavior.",
      buggy_code: w("    cout << \"Accessing an array within its valid bounds is a classic source of undefined behavior\" << endl;"),
      solution_code: w("    cout << \"Accessing an array out of its valid bounds is a classic source of undefined behavior\" << endl;"),
      expected_output: "Accessing an array out of its valid bounds is a classic source of undefined behavior",
      hints: ["Accessing within bounds is safe; it's accessing out of bounds that's undefined."],
      solution_summary: "The corrected statement accurately identifies out-of-bounds array access, not in-bounds access, as a classic UB source.",
      key_concepts: ["undefined behavior", "out-of-bounds access"], answer_bank: [w("    cout << \"Accessing an array out of its valid bounds is a classic source of undefined behavior\" << endl;")],
    },
    {
      id: "cpp_undefined_behavior_wc5", type: "output",
      prompt: "What does this code print, simulating UB examples as data?",
      snippet_code: w("    string examples[2] = {\"out-of-bounds array access\", \"using an uninitialized variable\"};\n    for (int i = 0; i < 2; i++) cout << examples[i] << endl;"),
      solution_code: w("    string examples[2] = {\"out-of-bounds array access\", \"using an uninitialized variable\"};\n    for (int i = 0; i < 2; i++) cout << examples[i] << endl;"),
      expected_output: "out-of-bounds array access\nusing an uninitialized variable",
      hints: ["The loop prints each example in array order."],
      solution_summary: "The loop iterates examples in order, printing each classic source of undefined behavior.",
      key_concepts: ["undefined behavior"], answer_bank: [],
    },
    {
      id: "cpp_undefined_behavior_wc6", type: "output",
      prompt: "What does this code print?",
      snippet_code: w("    bool isSafe = false;\n    cout << (isSafe ? \"defined behavior\" : \"potential undefined behavior\") << endl;"),
      solution_code: w("    bool isSafe = false;\n    cout << (isSafe ? \"defined behavior\" : \"potential undefined behavior\") << endl;"),
      expected_output: "potential undefined behavior",
      hints: ["isSafe is false, so the ternary picks the second option."],
      solution_summary: "Since isSafe is false, the ternary evaluates to \"potential undefined behavior\".",
      key_concepts: ["undefined behavior"], answer_bank: [],
    },
    {
      id: "cpp_undefined_behavior_wc7", type: "build",
      prompt: "Write a program storing two UB examples (\"dereferencing a null pointer\", \"signed integer overflow\") in an array and printing each as 'UB source: <example>'.",
      starter_code: w("        // declare the array and loop over it here"),
      solution_code: w("    string examples[2] = {\"dereferencing a null pointer\", \"signed integer overflow\"};\n    for (int i = 0; i < 2; i++) cout << \"UB source: \" << examples[i] << endl;"),
      expected_output: "UB source: dereferencing a null pointer\nUB source: signed integer overflow",
      hints: ["Loop through the array, formatting each example into the message."],
      solution_summary: "The loop iterates examples in order, printing each formatted as 'UB source: <example>'.",
      key_concepts: ["undefined behavior"],
      answer_bank: [w("    string examples[2] = {\"dereferencing a null pointer\", \"signed integer overflow\"};\n    for (int i = 0; i < 2; i++) cout << \"UB source: \" << examples[i] << endl;")],
    },
    {
      id: "cpp_undefined_behavior_wc8", type: "build",
      prompt: "Write a program printing two facts, one per line: that UB can pass tests on one machine, and that the C++ standard makes no guarantee about its outcome.",
      starter_code: w("    // print both facts here"),
      solution_code: w("    cout << \"UB can appear to pass all tests on one specific machine\" << endl;\n    cout << \"The C++ standard makes no guarantee about its actual outcome\" << endl;"),
      expected_output: "UB can appear to pass all tests on one specific machine\nThe C++ standard makes no guarantee about its actual outcome",
      hints: ["Use two separate cout statements, in order."],
      solution_summary: "Two cout statements state, in order, UB's deceptive test-passing behavior and the standard's lack of guarantee.",
      key_concepts: ["undefined behavior"],
      answer_bank: [w("    cout << \"UB can appear to pass all tests on one specific machine\" << endl;\n    cout << \"The C++ standard makes no guarantee about its actual outcome\" << endl;")],
    },
  ],
  cpp_build_systems: [
    {
      id: "cpp_build_systems_wc1", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating why CMake exists.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"C++ doesn't have one universal build tool built into the language\" << endl;", "    cout << \"CMake exists to manage builds across different platforms and compilers\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"C++ doesn't have one universal build tool built into the language\" << endl;\n    cout << \"CMake exists to manage builds across different platforms and compilers\" << endl;"),
      expected_output: "C++ doesn't have one universal build tool built into the language\nCMake exists to manage builds across different platforms and compilers",
      hints: ["The absence of a built-in tool is described before CMake's role filling that gap."],
      solution_summary: "C++'s lack of a built-in universal build tool is stated first, then CMake's role managing cross-platform builds.",
      key_concepts: ["CMake", "build systems"],
      answer_bank: [w("    cout << \"C++ doesn't have one universal build tool built into the language\" << endl;\n    cout << \"CMake exists to manage builds across different platforms and compilers\" << endl;")],
    },
    {
      id: "cpp_build_systems_wc2", type: "reorder",
      prompt: "Reorder these two blocks into a program narrating what CMake actually generates.",
      shuffled_lines: ["#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"A CMakeLists.txt file describes a project's build configuration\" << endl;", "    cout << \"CMake reads it and generates the actual platform-specific build files\" << endl;\n    return 0;\n}"],
      solution_code: w("    cout << \"A CMakeLists.txt file describes a project's build configuration\" << endl;\n    cout << \"CMake reads it and generates the actual platform-specific build files\" << endl;"),
      expected_output: "A CMakeLists.txt file describes a project's build configuration\nCMake reads it and generates the actual platform-specific build files",
      hints: ["The configuration file's contents are described before CMake reading and generating from it."],
      solution_summary: "CMakeLists.txt describes the build configuration first, then CMake reads it to generate platform-specific build files.",
      key_concepts: ["CMake", "CMakeLists.txt"],
      answer_bank: [w("    cout << \"A CMakeLists.txt file describes a project's build configuration\" << endl;\n    cout << \"CMake reads it and generates the actual platform-specific build files\" << endl;")],
    },
    {
      id: "cpp_build_systems_wc3", type: "fix",
      prompt: "Fix this code so it accurately describes what CMake is.",
      buggy_code: w("    cout << \"CMake is a C++ compiler that translates source directly into machine code\" << endl;"),
      solution_code: w("    cout << \"CMake is a build system generator, not a compiler itself\" << endl;"),
      expected_output: "CMake is a build system generator, not a compiler itself",
      hints: ["CMake generates build files for an underlying compiler/build tool — it doesn't compile code itself."],
      solution_summary: "The corrected statement accurately describes CMake as a build system generator, not a compiler.",
      key_concepts: ["CMake"], answer_bank: [w("    cout << \"CMake is a build system generator, not a compiler itself\" << endl;")],
    },
    {
      id: "cpp_build_systems_wc4", type: "fix",
      prompt: "Fix this code so it accurately describes why CMake is needed across platforms.",
      buggy_code: w("    cout << \"Every C++ compiler on every platform uses the exact same build commands\" << endl;"),
      solution_code: w("    cout << \"Different platforms and compilers use different build commands, which CMake abstracts over\" << endl;"),
      expected_output: "Different platforms and compilers use different build commands, which CMake abstracts over",
      hints: ["Build commands genuinely differ across platforms and compilers — that variance is exactly what CMake exists to abstract over."],
      solution_summary: "The corrected statement accurately describes real cross-platform build differences that CMake abstracts over.",
      key_concepts: ["CMake", "build systems"], answer_bank: [w("    cout << \"Different platforms and compilers use different build commands, which CMake abstracts over\" << endl;")],
    },
    {
      id: "cpp_build_systems_wc5", type: "output",
      prompt: "What does this code print, simulating build system facts as data?",
      snippet_code: w("    string facts[2] = {\"CMakeLists.txt\", \"cross-platform builds\"};\n    for (int i = 0; i < 2; i++) cout << facts[i] << endl;"),
      solution_code: w("    string facts[2] = {\"CMakeLists.txt\", \"cross-platform builds\"};\n    for (int i = 0; i < 2; i++) cout << facts[i] << endl;"),
      expected_output: "CMakeLists.txt\ncross-platform builds",
      hints: ["The loop prints each element of facts in order."],
      solution_summary: "The loop iterates facts in order, printing each CMake-related fact.",
      key_concepts: ["CMake"], answer_bank: [],
    },
    {
      id: "cpp_build_systems_wc6", type: "output",
      prompt: "What does this code print?",
      snippet_code: w("    bool usesCMake = true;\n    cout << (usesCMake ? \"CMakeLists.txt\" : \"no build config\") << endl;"),
      solution_code: w("    bool usesCMake = true;\n    cout << (usesCMake ? \"CMakeLists.txt\" : \"no build config\") << endl;"),
      expected_output: "CMakeLists.txt",
      hints: ["usesCMake is true, so the ternary picks the first option."],
      solution_summary: "Since usesCMake is true, the ternary evaluates to \"CMakeLists.txt\".",
      key_concepts: ["CMake"], answer_bank: [],
    },
    {
      id: "cpp_build_systems_wc7", type: "build",
      prompt: "Write a program storing build tool facts (\"CMake\", \"cross-platform\") in an array and printing each as 'build tooling: <fact>'.",
      starter_code: w("        // declare the array and loop over it here"),
      solution_code: w("    string facts[2] = {\"CMake\", \"cross-platform\"};\n    for (int i = 0; i < 2; i++) cout << \"build tooling: \" << facts[i] << endl;"),
      expected_output: "build tooling: CMake\nbuild tooling: cross-platform",
      hints: ["Loop through the array, formatting each fact into the message."],
      solution_summary: "The loop iterates facts in order, printing each formatted as 'build tooling: <fact>'.",
      key_concepts: ["CMake"],
      answer_bank: [w("    string facts[2] = {\"CMake\", \"cross-platform\"};\n    for (int i = 0; i < 2; i++) cout << \"build tooling: \" << facts[i] << endl;")],
    },
    {
      id: "cpp_build_systems_wc8", type: "build",
      prompt: "Write a program printing two facts, one per line: that CMakeLists.txt describes a project's build, and that CMake generates the actual platform-specific build files from it.",
      starter_code: w("    // print both facts here"),
      solution_code: w("    cout << \"CMakeLists.txt describes a project's build configuration\" << endl;\n    cout << \"CMake generates the actual platform-specific build files from it\" << endl;"),
      expected_output: "CMakeLists.txt describes a project's build configuration\nCMake generates the actual platform-specific build files from it",
      hints: ["Use two separate cout statements, in order."],
      solution_summary: "Two cout statements state, in order, what CMakeLists.txt describes and what CMake generates from it.",
      key_concepts: ["CMake", "CMakeLists.txt"],
      answer_bank: [w("    cout << \"CMakeLists.txt describes a project's build configuration\" << endl;\n    cout << \"CMake generates the actual platform-specific build files from it\" << endl;")],
    },
  ],
};

let added = 0;
for (const [topicId, challenges] of Object.entries(CONTENT)) {
  const topic = cpp.topics.find((t) => t.id === topicId);
  if (!topic) { console.error(`MISSING: ${topicId}`); continue; }
  if (!Array.isArray(topic.anvil_challenges)) topic.anvil_challenges = [];
  topic.anvil_challenges.push(...challenges);
  added += challenges.length;
}
fs.writeFileSync(KB_PATH, JSON.stringify(kb, null, 2) + "\n", "utf8");
console.log(`Added ${added} across ${Object.keys(CONTENT).length} topics.`);
