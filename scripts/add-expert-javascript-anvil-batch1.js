// Expert Anvil (JavaScript track) batch 1: all 10 empty JS topics.
// Matches existing js_json_native precedent: 8 challenges per topic
// (reorder, reorder, fix, fix, output, output, build, build), real executing
// JS via console.log, verified against Node.js.
//
// Judgment call, confirmed by direct testing against lib/codeExec.js's sandbox
// (new Function("console", code) called synchronously, postMessage sent
// immediately after fn() returns, before any microtask/macrotask queue ever
// flushes): Promise .then()/setTimeout/async-await continuations NEVER appear
// in captured output — only code that runs synchronously before the function
// returns is captured. Confirmed with: `console.log('a'); Promise.resolve()
// .then(()=>console.log('b')); console.log('c'));` → captured output is only
// "a\nc", 'b' never appears. This makes it impossible to correctly demonstrate
// real promise resolution or event loop ordering as executing code in this
// sandbox. Also no `document`/DOM, no React/Vue/lodash/dayjs bundled, and no
// real TypeScript compiler (type annotations are a SyntaxError in plain JS).
// So: js_promises_async, js_dom_manipulation, js_event_loop,
// js_react_vue_overview, js_npm_ecosystem, js_frontend_vs_backend_node,
// js_typescript_intro, js_common_libraries all use real, executing
// console.log()-narration of the facts instead. js_var_let_const and
// js_this_keyword use fully real, synchronously-executing JS.
const fs = require("fs");
const path = require("path");

const KB_PATH = path.join(__dirname, "..", "data", "knowledge_base.json");
const raw = fs.readFileSync(KB_PATH, "utf8");
const kb = JSON.parse(raw);
const tier = kb.tiers.find((t) => t.id === "expert");
const jsTrack = tier.language_tracks.javascript;

const CONTENT = {
  js_var_let_const: [
    {
      id: "js_var_let_const_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a working program showing that `let` is scoped to the block it's declared in.",
      shuffled_lines: [
        "console.log(typeof x);",
        "if (true) {",
        "  let x = 5;",
        "}",
      ],
      solution_code: "if (true) {\n  let x = 5;\n}\nconsole.log(typeof x);",
      expected_output: "undefined",
      hints: ["`x` is declared with `let` inside the if block, so it doesn't exist outside it.", "typeof on an undeclared variable returns \"undefined\" rather than throwing."],
      solution_summary: "Since `let x` is scoped to the if block, `x` doesn't exist outside it, so `typeof x` is \"undefined\".",
      key_concepts: ["let", "block scope"],
    },
    {
      id: "js_var_let_const_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a working program showing that `var` leaks out of a block, unlike `let`.",
      shuffled_lines: [
        "console.log(y);",
        "if (true) {",
        "  var y = 10;",
        "}",
      ],
      solution_code: "if (true) {\n  var y = 10;\n}\nconsole.log(y);",
      expected_output: "10",
      hints: ["`var` is function-scoped, not block-scoped, so it's visible outside the if block.", "This is the exact 'sticky note visible from every room' behavior var has."],
      solution_summary: "Because `var` ignores block scope, `y` remains accessible outside the if block, so console.log(y) prints 10.",
      key_concepts: ["var", "function scope"],
    },
    {
      id: "js_var_let_const_wc3",
      type: "fix",
      prompt: "Fix this code so reassigning a `const` variable no longer throws a TypeError.",
      broken_code: "const count = 1;\ncount = 2;\nconsole.log(count);",
      solution_code: "let count = 1;\ncount = 2;\nconsole.log(count);",
      expected_output: "2",
      hints: ["`const` bindings can never be reassigned after their initial value.", "Switching to `let` allows reassignment while keeping block scoping."],
      solution_summary: "Changing const to let allows count to be reassigned to 2, since let (unlike const) permits reassignment.",
      key_concepts: ["const", "let", "reassignment"],
    },
    {
      id: "js_var_let_const_wc4",
      type: "fix",
      prompt: "Fix this code so it no longer throws a ReferenceError from using a `let` variable before its declaration.",
      broken_code: "console.log(name);\nlet name = \"Kai\";",
      solution_code: "let name = \"Kai\";\nconsole.log(name);",
      expected_output: "Kai",
      hints: ["`let` variables exist in a 'temporal dead zone' before their declaration line runs.", "Moving the declaration above the console.log fixes the ordering."],
      solution_summary: "Moving `let name = \"Kai\"` before console.log(name) avoids the temporal dead zone ReferenceError, printing \"Kai\".",
      key_concepts: ["let", "temporal dead zone"],
    },
    {
      id: "js_var_let_const_wc5",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "for (let i = 0; i < 3; i++) {\n  if (i === 1) {\n    let i = 100;\n    console.log(\"inner:\", i);\n  }\n}\nconsole.log(\"final i check complete\");",
      expected_output: "inner: 100\nfinal i check complete",
      hints: ["The inner `let i` shadows the loop's `i` only within that if block.", "The loop's own `i` is unaffected by the inner shadowed variable."],
      solution_summary: "The inner `let i = 100` shadows the outer loop variable only inside the if block, printing 'inner: 100' once when i===1, then the final line prints after the loop completes.",
      key_concepts: ["let", "variable shadowing"],
    },
    {
      id: "js_var_let_const_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "const person = { name: \"Ari\" };\nperson.name = \"Sam\";\nconsole.log(person.name);",
      expected_output: "Sam",
      hints: ["`const` prevents reassigning the variable itself, not mutating properties of an object it points to."],
      solution_summary: "const only prevents reassigning `person` itself; mutating person.name is still allowed, so it prints 'Sam'.",
      key_concepts: ["const", "object mutation"],
    },
    {
      id: "js_var_let_const_wc7",
      type: "build",
      prompt: "Write code using `let` inside a for loop's block that logs each loop index, then a `const` outside the loop that logs a fixed label, in that order.",
      solution_code: "for (let i = 0; i < 2; i++) {\n  console.log(i);\n}\nconst label = \"done\";\nconsole.log(label);",
      expected_output: "0\n1\ndone",
      hints: ["`let i` inside the for loop is scoped to each iteration.", "Declare `label` with const after the loop finishes."],
      solution_summary: "The loop logs 0 then 1 using its `let i`, then the const label logs 'done' after the loop completes.",
      key_concepts: ["let", "const", "loop scope"],
    },
    {
      id: "js_var_let_const_wc8",
      type: "build",
      prompt: "Write code that declares a `const` array, then uses `.push()` to add an element to it (allowed, since push mutates rather than reassigns), and logs the array.",
      solution_code: "const items = [\"a\", \"b\"];\nitems.push(\"c\");\nconsole.log(items);",
      expected_output: "[\"a\",\"b\",\"c\"]",
      hints: ["const prevents reassigning `items` to a new array, but .push() mutates the existing array in place, which is allowed."],
      solution_summary: "push() mutates the existing const array in place rather than reassigning it, so items ends up as [\"a\",\"b\",\"c\"].",
      key_concepts: ["const", "array mutation"],
    },
  ],
  js_promises_async: [
    {
      id: "js_promises_async_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating what a Promise represents.",
      shuffled_lines: [
        "console.log(\"A Promise represents a value that will exist eventually, not one that exists right now.\");",
        "console.log(\"It specifically represents the eventual result of an asynchronous operation, like a network request.\");",
        "console.log(\"That operation takes some real amount of time rather than finishing instantly.\");",
      ],
      solution_code: "console.log(\"A Promise represents a value that will exist eventually, not one that exists right now.\");\nconsole.log(\"It specifically represents the eventual result of an asynchronous operation, like a network request.\");\nconsole.log(\"That operation takes some real amount of time rather than finishing instantly.\");",
      expected_output: "A Promise represents a value that will exist eventually, not one that exists right now.\nIt specifically represents the eventual result of an asynchronous operation, like a network request.\nThat operation takes some real amount of time rather than finishing instantly.",
      hints: ["The general definition of a Promise comes before the specific example of what it represents.", "The timing detail (takes real time) is the final, most concrete point."],
      solution_summary: "A Promise represents an eventual value, specifically the result of an async operation like a network request, which takes real time rather than finishing instantly.",
      key_concepts: ["Promise", "asynchronous operation"],
    },
    {
      id: "js_promises_async_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating a Promise's three possible states.",
      shuffled_lines: [
        "console.log(\"Pending: the operation hasn't finished yet.\");",
        "console.log(\"Fulfilled: the operation completed successfully with a value.\");",
        "console.log(\"Rejected: the operation failed with an error.\");",
      ],
      solution_code: "console.log(\"Pending: the operation hasn't finished yet.\");\nconsole.log(\"Fulfilled: the operation completed successfully with a value.\");\nconsole.log(\"Rejected: the operation failed with an error.\");",
      expected_output: "Pending: the operation hasn't finished yet.\nFulfilled: the operation completed successfully with a value.\nRejected: the operation failed with an error.",
      hints: ["Pending is the starting state before either final outcome.", "Fulfilled and rejected are the two possible final states a Promise can settle into."],
      solution_summary: "A Promise starts pending, and settles into either fulfilled (success with a value) or rejected (failure with an error).",
      key_concepts: ["Promise", "pending", "fulfilled", "rejected"],
    },
    {
      id: "js_promises_async_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what `.then()` does, rather than claiming it blocks execution until the Promise resolves.",
      broken_code: "console.log(\".then() pauses all other code from running until the Promise resolves.\");",
      solution_code: "console.log(\".then() registers a callback to run once the Promise resolves, without blocking other code.\");",
      expected_output: ".then() registers a callback to run once the Promise resolves, without blocking other code.",
      hints: [".then() doesn't block anything — it schedules a callback for later, once the Promise settles.", "Other synchronous code keeps running immediately, unaffected by pending Promises."],
      solution_summary: "The corrected statement accurately describes .then() as registering a callback for later, without blocking other code from running.",
      key_concepts: ["Promise", ".then()", "non-blocking"],
    },
    {
      id: "js_promises_async_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes async/await's relationship to Promises.",
      broken_code: "console.log(\"async/await is a completely different mechanism from Promises, unrelated to them.\");",
      solution_code: "console.log(\"async/await is syntax sugar built on top of Promises, making asynchronous code read like synchronous code.\");",
      expected_output: "async/await is syntax sugar built on top of Promises, making asynchronous code read like synchronous code.",
      hints: ["An async function always returns a Promise under the hood.", "await simply pauses within that async function until a Promise settles."],
      solution_summary: "The corrected statement accurately describes async/await as syntax sugar over Promises, not an unrelated separate mechanism.",
      key_concepts: ["async/await", "Promise"],
    },
    {
      id: "js_promises_async_wc5",
      type: "output",
      prompt: "What does this code print? (Only synchronous code runs before this script's output is captured — anything scheduled for later, like inside .then(), never appears.)",
      solution_code: "console.log(\"first\");\nconsole.log(\"second\");\nconsole.log(\"third\");",
      expected_output: "first\nsecond\nthird",
      hints: ["Each console.log runs synchronously, one after another, in the exact order written."],
      solution_summary: "All three console.log calls run synchronously in order, printing 'first', 'second', then 'third'.",
      key_concepts: ["synchronous execution", "Promise"],
    },
    {
      id: "js_promises_async_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "const states = [\"pending\", \"fulfilled\", \"rejected\"];\nfor (const state of states) {\n  console.log(`Promise state: ${state}`);\n}",
      expected_output: "Promise state: pending\nPromise state: fulfilled\nPromise state: rejected",
      hints: ["The for...of loop iterates the array in order, formatting each state into the template string."],
      solution_summary: "The loop iterates the states array in order, printing each Promise state formatted into the template string.",
      key_concepts: ["Promise", "pending", "fulfilled", "rejected"],
    },
    {
      id: "js_promises_async_wc7",
      type: "build",
      prompt: "Write code that logs each of a Promise's three states from an array, formatted as '<state> is a possible Promise state'.",
      solution_code: "const states = [\"pending\", \"fulfilled\", \"rejected\"];\nfor (const state of states) {\n  console.log(`${state} is a possible Promise state`);\n}",
      expected_output: "pending is a possible Promise state\nfulfilled is a possible Promise state\nrejected is a possible Promise state",
      hints: ["Use a for...of loop over the states array, formatting each one into a template string."],
      solution_summary: "The loop iterates the states array in order, printing each one formatted into the message.",
      key_concepts: ["Promise", "pending", "fulfilled", "rejected"],
    },
    {
      id: "js_promises_async_wc8",
      type: "build",
      prompt: "Write code that logs three facts about async/await, one per line: that it's built on Promises, that await pauses within an async function, and that it makes async code read like synchronous code.",
      solution_code: "console.log(\"async/await is built on top of Promises.\");\nconsole.log(\"await pauses execution within an async function until a Promise settles.\");\nconsole.log(\"This makes asynchronous code read like ordinary synchronous code.\");",
      expected_output: "async/await is built on top of Promises.\nawait pauses execution within an async function until a Promise settles.\nThis makes asynchronous code read like ordinary synchronous code.",
      hints: ["Each fact should be its own console.log call, in the order the facts naturally build on each other."],
      solution_summary: "Three console.log calls state, in order, that async/await is built on Promises, that await pauses for a settled Promise, and that this makes async code read synchronously.",
      key_concepts: ["async/await", "Promise"],
    },
  ],
  js_dom_manipulation: [
    {
      id: "js_dom_manipulation_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating what the DOM represents.",
      shuffled_lines: [
        "console.log(\"The DOM is the browser's live, in-memory representation of a webpage's structure.\");",
        "console.log(\"Every HTML element on the page exists as a corresponding object within this structure.\");",
        "console.log(\"These objects are organized into a tree reflecting how elements are nested inside one another.\");",
      ],
      solution_code: "console.log(\"The DOM is the browser's live, in-memory representation of a webpage's structure.\");\nconsole.log(\"Every HTML element on the page exists as a corresponding object within this structure.\");\nconsole.log(\"These objects are organized into a tree reflecting how elements are nested inside one another.\");",
      expected_output: "The DOM is the browser's live, in-memory representation of a webpage's structure.\nEvery HTML element on the page exists as a corresponding object within this structure.\nThese objects are organized into a tree reflecting how elements are nested inside one another.",
      hints: ["The general definition of the DOM comes first.", "The nested tree structure is the most specific, final detail."],
      solution_summary: "The DOM is the browser's live in-memory page structure, where each HTML element is an object, organized into a nested tree.",
      key_concepts: ["DOM", "Document Object Model"],
    },
    {
      id: "js_dom_manipulation_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating a typical DOM manipulation workflow.",
      shuffled_lines: [
        "console.log(\"Step 3: the browser re-renders the page reflecting that change.\");",
        "console.log(\"Step 1: select an element in the DOM, such as by its id.\");",
        "console.log(\"Step 2: modify that element, such as changing its text content.\");",
      ],
      solution_code: "console.log(\"Step 1: select an element in the DOM, such as by its id.\");\nconsole.log(\"Step 2: modify that element, such as changing its text content.\");\nconsole.log(\"Step 3: the browser re-renders the page reflecting that change.\");",
      expected_output: "Step 1: select an element in the DOM, such as by its id.\nStep 2: modify that element, such as changing its text content.\nStep 3: the browser re-renders the page reflecting that change.",
      hints: ["An element must be selected before it can be modified.", "The browser re-renders only after the modification actually happens."],
      solution_summary: "A DOM element is first selected, then modified, then the browser re-renders the page to reflect that change.",
      key_concepts: ["DOM manipulation", "selecting elements"],
    },
    {
      id: "js_dom_manipulation_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what document.getElementById does, rather than claiming it modifies the page directly.",
      broken_code: "console.log(\"document.getElementById immediately changes the text of every element on the page.\");",
      solution_code: "console.log(\"document.getElementById selects and returns one specific element from the DOM by its id.\");",
      expected_output: "document.getElementById selects and returns one specific element from the DOM by its id.",
      hints: ["getElementById is a selection method, not a modification method.", "It returns a reference to one specific element found by its id."],
      solution_summary: "The corrected statement accurately describes getElementById as selecting and returning one specific element by id, not modifying anything.",
      key_concepts: ["DOM", "getElementById"],
    },
    {
      id: "js_dom_manipulation_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what happens when you set an element's textContent property.",
      broken_code: "console.log(\"Setting textContent only changes the in-memory DOM object, with no visible effect on the page.\");",
      solution_code: "console.log(\"Setting textContent changes the in-memory DOM object, and the browser re-renders the visible page to match.\");",
      expected_output: "Setting textContent changes the in-memory DOM object, and the browser re-renders the visible page to match.",
      hints: ["The DOM being 'live' means changes to it are reflected visually on the actual page.", "The browser automatically re-renders after a DOM change."],
      solution_summary: "The corrected statement accurately describes that changing textContent updates the DOM and the browser re-renders the visible page to match.",
      key_concepts: ["DOM", "textContent", "re-rendering"],
    },
    {
      id: "js_dom_manipulation_wc5",
      type: "output",
      prompt: "What does this code print, simulating a DOM tree as a nested object?",
      solution_code: "const domTree = { tag: \"div\", children: [{ tag: \"p\" }, { tag: \"span\" }] };\nfor (const child of domTree.children) {\n  console.log(child.tag);\n}",
      expected_output: "p\nspan",
      hints: ["The for...of loop iterates domTree.children in order.", "Each child object's tag property is printed."],
      solution_summary: "The loop iterates domTree.children in order, printing each child's tag: 'p' then 'span'.",
      key_concepts: ["DOM", "nested tree structure"],
    },
    {
      id: "js_dom_manipulation_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function selectElement(id, elements) {\n  return elements.find((el) => el.id === id);\n}\n\nconst elements = [{ id: \"title\" }, { id: \"footer\" }];\nconsole.log(selectElement(\"footer\", elements));",
      expected_output: "{\"id\":\"footer\"}",
      hints: ["Array.prototype.find returns the first element matching the condition.", "console.log on an object prints its JSON-like representation."],
      solution_summary: "selectElement finds the object with id 'footer' in the elements array and returns it, printed as {\"id\":\"footer\"}.",
      key_concepts: ["DOM", "selecting elements"],
    },
    {
      id: "js_dom_manipulation_wc7",
      type: "build",
      prompt: "Write code representing a simplified DOM node as an object with a `tag` and `text` property, then log a message in the format 'Selected <tag>: <text>'.",
      solution_code: "const node = { tag: \"h1\", text: \"Welcome\" };\nconsole.log(`Selected ${node.tag}: ${node.text}`);",
      expected_output: "Selected h1: Welcome",
      hints: ["Use a template string to format both properties into one message."],
      solution_summary: "The template string formats node.tag and node.text together, printing 'Selected h1: Welcome'.",
      key_concepts: ["DOM", "selecting elements"],
    },
    {
      id: "js_dom_manipulation_wc8",
      type: "build",
      prompt: "Write code simulating updating a DOM node's text: start with a node object with text 'old', update it to 'new', then log the updated text.",
      solution_code: "const node = { text: \"old\" };\nnode.text = \"new\";\nconsole.log(node.text);",
      expected_output: "new",
      hints: ["Mutate the node's text property directly, then log the updated value."],
      solution_summary: "node.text is reassigned from 'old' to 'new', and logging node.text afterward prints 'new'.",
      key_concepts: ["DOM manipulation", "textContent"],
    },
  ],
  js_event_loop: [
    {
      id: "js_event_loop_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating why JavaScript's single-thread design matters.",
      shuffled_lines: [
        "console.log(\"JavaScript runs on a single main thread.\");",
        "console.log(\"Only one thread can actually execute JavaScript code at any given moment.\");",
        "console.log(\"This is in sharp contrast to languages supporting true multi-threaded parallel execution.\");",
      ],
      solution_code: "console.log(\"JavaScript runs on a single main thread.\");\nconsole.log(\"Only one thread can actually execute JavaScript code at any given moment.\");\nconsole.log(\"This is in sharp contrast to languages supporting true multi-threaded parallel execution.\");",
      expected_output: "JavaScript runs on a single main thread.\nOnly one thread can actually execute JavaScript code at any given moment.\nThis is in sharp contrast to languages supporting true multi-threaded parallel execution.",
      hints: ["The basic fact (single thread) is stated before its specific consequence.", "The contrast with multi-threaded languages is the final, broader point."],
      solution_summary: "JavaScript runs on a single main thread, meaning only one thread executes code at a time, unlike languages with true multi-threaded parallelism.",
      key_concepts: ["event loop", "single-threaded"],
    },
    {
      id: "js_event_loop_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating how the event loop processes queued callbacks after synchronous code finishes.",
      shuffled_lines: [
        "console.log(\"All currently running synchronous code finishes executing first.\");",
        "console.log(\"The event loop then checks the queue for any waiting callbacks.\");",
        "console.log(\"A queued callback, such as one from setTimeout, runs once the queue is checked.\");",
      ],
      solution_code: "console.log(\"All currently running synchronous code finishes executing first.\");\nconsole.log(\"The event loop then checks the queue for any waiting callbacks.\");\nconsole.log(\"A queued callback, such as one from setTimeout, runs once the queue is checked.\");",
      expected_output: "All currently running synchronous code finishes executing first.\nThe event loop then checks the queue for any waiting callbacks.\nA queued callback, such as one from setTimeout, runs once the queue is checked.",
      hints: ["Synchronous code always finishes before the event loop checks the queue.", "A queued callback only runs after that check happens."],
      solution_summary: "Synchronous code finishes first, then the event loop checks the queue, then a queued callback like setTimeout's actually runs.",
      key_concepts: ["event loop", "callback queue"],
    },
    {
      id: "js_event_loop_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes when a setTimeout callback actually runs, rather than claiming it runs immediately.",
      broken_code: "console.log(\"A setTimeout(fn, 0) callback runs immediately, before any other synchronous code.\");",
      solution_code: "console.log(\"A setTimeout(fn, 0) callback is queued and only runs after all current synchronous code finishes.\");",
      expected_output: "A setTimeout(fn, 0) callback is queued and only runs after all current synchronous code finishes.",
      hints: ["Even with a 0ms delay, setTimeout's callback is queued, not run immediately.", "Synchronous code always finishes running before any queued callback gets its turn."],
      solution_summary: "The corrected statement accurately describes setTimeout(fn, 0) as queuing its callback for after current synchronous code finishes, not running immediately.",
      key_concepts: ["event loop", "setTimeout"],
    },
    {
      id: "js_event_loop_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what blocks the event loop.",
      broken_code: "console.log(\"Only network requests can ever block the JavaScript event loop.\");",
      solution_code: "console.log(\"Any long-running synchronous code blocks the event loop, since JavaScript runs on a single thread.\");",
      expected_output: "Any long-running synchronous code blocks the event loop, since JavaScript runs on a single thread.",
      hints: ["Since only one thread runs JS code, any slow synchronous operation blocks everything else, not just network calls.", "The single-thread design is exactly why long synchronous work is a real problem."],
      solution_summary: "The corrected statement accurately describes that any long-running synchronous code blocks the event loop, since JavaScript has only one thread to run it on.",
      key_concepts: ["event loop", "single-threaded", "blocking"],
    },
    {
      id: "js_event_loop_wc5",
      type: "output",
      prompt: "What does this code print? (Only synchronous code is captured before output is read — this demonstrates that synchronous code always finishes before anything queued would run.)",
      solution_code: "console.log(\"sync 1\");\nconsole.log(\"sync 2\");\nconsole.log(\"sync 3\");",
      expected_output: "sync 1\nsync 2\nsync 3",
      hints: ["All three lines are synchronous console.log calls, executing immediately in order."],
      solution_summary: "All three synchronous console.log calls run immediately in the order written, printing 'sync 1', 'sync 2', 'sync 3'.",
      key_concepts: ["event loop", "synchronous execution"],
    },
    {
      id: "js_event_loop_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "const queue = [\"timer callback\", \"click handler\", \"fetch callback\"];\nfor (const item of queue) {\n  console.log(`queued: ${item}`);\n}",
      expected_output: "queued: timer callback\nqueued: click handler\nqueued: fetch callback",
      hints: ["The for...of loop iterates the queue array in order, formatting each item."],
      solution_summary: "The loop iterates the queue array in order, printing each queued item formatted into the message.",
      key_concepts: ["event loop", "callback queue"],
    },
    {
      id: "js_event_loop_wc7",
      type: "build",
      prompt: "Write code that logs three synchronous statements in order describing the event loop's three-part cycle: run synchronous code, check the queue, run a queued callback.",
      solution_code: "console.log(\"1. Run all currently pending synchronous code.\");\nconsole.log(\"2. Check the queue for any waiting callbacks.\");\nconsole.log(\"3. Run the next queued callback, then repeat.\");",
      expected_output: "1. Run all currently pending synchronous code.\n2. Check the queue for any waiting callbacks.\n3. Run the next queued callback, then repeat.",
      hints: ["Each statement should describe one step of the cycle, in the order the cycle actually happens."],
      solution_summary: "Three console.log calls describe, in order, running synchronous code, checking the queue, and running the next queued callback.",
      key_concepts: ["event loop", "callback queue"],
    },
    {
      id: "js_event_loop_wc8",
      type: "build",
      prompt: "Write code that logs each item in an array of queued callback names, formatted as 'waiting to run: <name>'.",
      solution_code: "const callbacks = [\"onClick\", \"onLoad\", \"onTimeout\"];\nfor (const name of callbacks) {\n  console.log(`waiting to run: ${name}`);\n}",
      expected_output: "waiting to run: onClick\nwaiting to run: onLoad\nwaiting to run: onTimeout",
      hints: ["Loop through the callbacks array and format each name into the message."],
      solution_summary: "The loop iterates the callbacks array in order, printing each queued callback name formatted into the message.",
      key_concepts: ["event loop", "callback queue"],
    },
  ],
  js_react_vue_overview: [
    {
      id: "js_react_vue_overview_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating the shared core idea behind React and Vue.",
      shuffled_lines: [
        "console.log(\"React and Vue are the two dominant frameworks for building interactive user interfaces.\");",
        "console.log(\"Both describe an interface as a collection of reusable components.\");",
        "console.log(\"Each component specifies what a piece of the UI should look like given its current data.\");",
      ],
      solution_code: "console.log(\"React and Vue are the two dominant frameworks for building interactive user interfaces.\");\nconsole.log(\"Both describe an interface as a collection of reusable components.\");\nconsole.log(\"Each component specifies what a piece of the UI should look like given its current data.\");",
      expected_output: "React and Vue are the two dominant frameworks for building interactive user interfaces.\nBoth describe an interface as a collection of reusable components.\nEach component specifies what a piece of the UI should look like given its current data.",
      hints: ["The general framing of React and Vue comes first.", "The specific description of what a component does is the most concrete, final detail."],
      solution_summary: "React and Vue are described as dominant UI frameworks, both built around reusable components that describe UI based on current data.",
      key_concepts: ["React", "Vue", "components"],
    },
    {
      id: "js_react_vue_overview_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating why component-based UI avoids manually updating the page step by step.",
      shuffled_lines: [
        "console.log(\"A component re-renders automatically whenever its underlying data changes.\");",
        "console.log(\"Traditionally, a developer manually updates each piece of the page step by step.\");",
        "console.log(\"This removes the need to manually track every individual DOM update by hand.\");",
      ],
      solution_code: "console.log(\"Traditionally, a developer manually updates each piece of the page step by step.\");\nconsole.log(\"A component re-renders automatically whenever its underlying data changes.\");\nconsole.log(\"This removes the need to manually track every individual DOM update by hand.\");",
      expected_output: "Traditionally, a developer manually updates each piece of the page step by step.\nA component re-renders automatically whenever its underlying data changes.\nThis removes the need to manually track every individual DOM update by hand.",
      hints: ["The old manual approach is described before the automatic component-based alternative.", "The final line states the benefit gained from that automatic approach."],
      solution_summary: "The old manual approach is contrasted with components automatically re-rendering on data change, removing the need for manual DOM tracking.",
      key_concepts: ["React", "Vue", "re-rendering"],
    },
    {
      id: "js_react_vue_overview_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what a component is, rather than claiming it's a single, one-time-use page.",
      broken_code: "console.log(\"A component is a single, one-time-use page that can never be reused elsewhere.\");",
      solution_code: "console.log(\"A component is a reusable piece of UI that can be used in multiple places throughout an app.\");",
      expected_output: "A component is a reusable piece of UI that can be used in multiple places throughout an app.",
      hints: ["Reusability is the defining feature of a component, not one-time use.", "The same component can appear in many different places in an app."],
      solution_summary: "The corrected statement accurately describes a component as reusable, not a one-time-use page.",
      key_concepts: ["React", "Vue", "components"],
    },
    {
      id: "js_react_vue_overview_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what triggers a component to re-render.",
      broken_code: "console.log(\"A component re-renders only when a developer manually reloads the entire page.\");",
      solution_code: "console.log(\"A component re-renders automatically whenever its underlying data changes.\");",
      expected_output: "A component re-renders automatically whenever its underlying data changes.",
      hints: ["Components re-render automatically based on data changes, not manual page reloads.", "This automatic behavior is exactly what removes the need for manual DOM updates."],
      solution_summary: "The corrected statement accurately describes automatic re-rendering triggered by data changes, not a manual full page reload.",
      key_concepts: ["React", "Vue", "re-rendering"],
    },
    {
      id: "js_react_vue_overview_wc5",
      type: "output",
      prompt: "What does this code print, simulating rendering a list of components?",
      solution_code: "const components = [\"Header\", \"Sidebar\", \"Footer\"];\nfor (const component of components) {\n  console.log(`rendering: ${component}`);\n}",
      expected_output: "rendering: Header\nrendering: Sidebar\nrendering: Footer",
      hints: ["The for...of loop iterates the components array in order, formatting each name."],
      solution_summary: "The loop iterates the components array in order, printing a 'rendering' line for each component name.",
      key_concepts: ["React", "Vue", "components"],
    },
    {
      id: "js_react_vue_overview_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function render(data) {\n  return `showing: ${data.title}`;\n}\n\nconsole.log(render({ title: \"Home Page\" }));",
      expected_output: "showing: Home Page",
      hints: ["render receives an object and formats its title property into a template string."],
      solution_summary: "render({ title: \"Home Page\" }) formats data.title into the template string, printing 'showing: Home Page'.",
      key_concepts: ["React", "Vue", "component data"],
    },
    {
      id: "js_react_vue_overview_wc7",
      type: "build",
      prompt: "Write code representing a simplified component as a function that takes a `name` and returns a string '<name> component rendered', then log the result for \"Navbar\".",
      solution_code: "function renderComponent(name) {\n  return `${name} component rendered`;\n}\n\nconsole.log(renderComponent(\"Navbar\"));",
      expected_output: "Navbar component rendered",
      hints: ["Use a template string to insert the name parameter into the returned message."],
      solution_summary: "renderComponent formats the name parameter into the returned string, producing 'Navbar component rendered' for \"Navbar\".",
      key_concepts: ["React", "Vue", "components"],
    },
    {
      id: "js_react_vue_overview_wc8",
      type: "build",
      prompt: "Write code storing three component names in an array and logging each formatted as '<name> is a reusable component'.",
      solution_code: "const components = [\"Button\", \"Card\", \"Modal\"];\nfor (const name of components) {\n  console.log(`${name} is a reusable component`);\n}",
      expected_output: "Button is a reusable component\nCard is a reusable component\nModal is a reusable component",
      hints: ["Loop through the components array and format each name into the message."],
      solution_summary: "The loop iterates the components array in order, printing each name formatted into the reusability message.",
      key_concepts: ["React", "Vue", "components"],
    },
  ],
  js_npm_ecosystem: [
    {
      id: "js_npm_ecosystem_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating why npm hosts such a large package ecosystem.",
      shuffled_lines: [
        "console.log(\"npm hosts the largest package ecosystem of any programming language in widespread use.\");",
        "console.log(\"JavaScript is unusually dominant on both the frontend and the backend at once.\");",
        "console.log(\"That dual dominance drives an especially large number of published packages.\");",
      ],
      solution_code: "console.log(\"npm hosts the largest package ecosystem of any programming language in widespread use.\");\nconsole.log(\"JavaScript is unusually dominant on both the frontend and the backend at once.\");\nconsole.log(\"That dual dominance drives an especially large number of published packages.\");",
      expected_output: "npm hosts the largest package ecosystem of any programming language in widespread use.\nJavaScript is unusually dominant on both the frontend and the backend at once.\nThat dual dominance drives an especially large number of published packages.",
      hints: ["The overall claim about npm's size comes first, followed by the underlying reason.", "The consequence of dual dominance is the final, most specific point."],
      solution_summary: "npm hosts the largest ecosystem, driven by JavaScript's dual dominance on frontend and backend, which drives an especially large number of packages.",
      key_concepts: ["npm", "package ecosystem"],
    },
    {
      id: "js_npm_ecosystem_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating a typical npm install workflow.",
      shuffled_lines: [
        "console.log(\"Step 3: the package appears in the project's node_modules folder.\");",
        "console.log(\"Step 1: run npm install <package-name> in a project's directory.\");",
        "console.log(\"Step 2: npm downloads that package and its own dependencies from the registry.\");",
      ],
      solution_code: "console.log(\"Step 1: run npm install <package-name> in a project's directory.\");\nconsole.log(\"Step 2: npm downloads that package and its own dependencies from the registry.\");\nconsole.log(\"Step 3: the package appears in the project's node_modules folder.\");",
      expected_output: "Step 1: run npm install <package-name> in a project's directory.\nStep 2: npm downloads that package and its own dependencies from the registry.\nStep 3: the package appears in the project's node_modules folder.",
      hints: ["Running the install command comes before anything is downloaded.", "The package appearing in node_modules is the final result of the download step."],
      solution_summary: "npm install is run first, then npm downloads the package and its dependencies, then the package appears in node_modules.",
      key_concepts: ["npm", "node_modules"],
    },
    {
      id: "js_npm_ecosystem_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what package.json is for.",
      broken_code: "console.log(\"package.json stores the actual compiled binary code of every installed package.\");",
      solution_code: "console.log(\"package.json lists a project's metadata and its declared dependencies and their versions.\");",
      expected_output: "package.json lists a project's metadata and its declared dependencies and their versions.",
      hints: ["package.json is a manifest file listing dependencies, not compiled binary code.", "The actual installed code lives in node_modules, not package.json itself."],
      solution_summary: "The corrected statement accurately describes package.json as a manifest listing metadata and declared dependencies, not compiled binaries.",
      key_concepts: ["npm", "package.json"],
    },
    {
      id: "js_npm_ecosystem_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what node_modules contains.",
      broken_code: "console.log(\"node_modules contains only the project's own source code, never any external packages.\");",
      solution_code: "console.log(\"node_modules contains the actual downloaded code for every installed external package.\");",
      expected_output: "node_modules contains the actual downloaded code for every installed external package.",
      hints: ["node_modules is specifically where npm places downloaded external packages.", "It doesn't hold the project's own hand-written source code — that lives elsewhere in the project."],
      solution_summary: "The corrected statement accurately describes node_modules as holding the downloaded code for installed external packages, not the project's own source.",
      key_concepts: ["npm", "node_modules"],
    },
    {
      id: "js_npm_ecosystem_wc5",
      type: "output",
      prompt: "What does this code print, simulating package.json's dependency list?",
      solution_code: "const dependencies = { react: \"^18.0.0\", lodash: \"^4.17.0\" };\nfor (const [name, version] of Object.entries(dependencies)) {\n  console.log(`${name}: ${version}`);\n}",
      expected_output: "react: ^18.0.0\nlodash: ^4.17.0",
      hints: ["Object.entries returns [key, value] pairs in insertion order.", "Destructuring [name, version] gives access to both parts of each pair."],
      solution_summary: "Object.entries iterates the dependencies object in order, printing each package name alongside its version.",
      key_concepts: ["npm", "package.json"],
    },
    {
      id: "js_npm_ecosystem_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function describePackage(name) {\n  return `${name} is downloaded into node_modules`;\n}\n\nconsole.log(describePackage(\"express\"));",
      expected_output: "express is downloaded into node_modules",
      hints: ["describePackage formats the name parameter into the returned message."],
      solution_summary: "describePackage(\"express\") formats the name into the template string, printing 'express is downloaded into node_modules'.",
      key_concepts: ["npm", "node_modules"],
    },
    {
      id: "js_npm_ecosystem_wc7",
      type: "build",
      prompt: "Write code storing two packages and their versions in an object, then log each formatted as '<name>@<version>' using Object.entries.",
      solution_code: "const dependencies = { axios: \"1.6.0\", dayjs: \"1.11.0\" };\nfor (const [name, version] of Object.entries(dependencies)) {\n  console.log(`${name}@${version}`);\n}",
      expected_output: "axios@1.6.0\ndayjs@1.11.0",
      hints: ["Object.entries gives you [name, version] pairs to destructure directly in the loop."],
      solution_summary: "Object.entries iterates the dependencies object in order, printing each package formatted as name@version.",
      key_concepts: ["npm", "package.json"],
    },
    {
      id: "js_npm_ecosystem_wc8",
      type: "build",
      prompt: "Write a function `installMessage(name)` that returns 'installing <name> from the npm registry', then log the result for \"chalk\".",
      solution_code: "function installMessage(name) {\n  return `installing ${name} from the npm registry`;\n}\n\nconsole.log(installMessage(\"chalk\"));",
      expected_output: "installing chalk from the npm registry",
      hints: ["Use a template string to insert the name parameter into the returned message."],
      solution_summary: "installMessage(\"chalk\") formats the name into the returned string, printing 'installing chalk from the npm registry'.",
      key_concepts: ["npm", "package registry"],
    },
  ],
  js_this_keyword: [
    {
      id: "js_this_keyword_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a working program showing that `this` refers to the object a method is called on.",
      shuffled_lines: [
        "console.log(user.greet());",
        "const user = {",
        "  name: \"Kai\",",
        "  greet() {",
        "    return `Hi, I'm ${this.name}`;",
        "  },",
        "};",
      ],
      solution_code: "const user = {\n  name: \"Kai\",\n  greet() {\n    return `Hi, I'm ${this.name}`;\n  },\n};\n\nconsole.log(user.greet());",
      expected_output: "Hi, I'm Kai",
      hints: ["The object must be defined before its method can be called.", "Inside greet(), `this` refers to `user`, since greet is called as user.greet()."],
      solution_summary: "Since greet() is called as user.greet(), `this` refers to user, so this.name is 'Kai', printing \"Hi, I'm Kai\".",
      key_concepts: ["this", "method call"],
    },
    {
      id: "js_this_keyword_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a working program showing that a regular function's `this` is not the object it's later assigned to.",
      shuffled_lines: [
        "const greetFn = person.greet;",
        "const person = {",
        "  name: \"Sam\",",
        "  greet() {",
        "    return this === undefined ? \"no this\" : this.name;",
        "  },",
        "};",
        "console.log(typeof greetFn());",
      ],
      solution_code: "const person = {\n  name: \"Sam\",\n  greet() {\n    return this === undefined ? \"no this\" : this.name;\n  },\n};\nconst greetFn = person.greet;\nconsole.log(typeof greetFn());",
      expected_output: "undefined",
      hints: ["When greetFn() is called standalone, `this` is no longer bound to `person` — in non-strict code it falls back to the global object, not literally `undefined`.", "Since the global object has no `name` property, this.name is undefined, and typeof undefined is the string 'undefined'."],
      solution_summary: "greetFn() is called standalone, detached from person; `this` falls back to the global object (not `undefined` itself), so this.name is undefined, and typeof that result is the string 'undefined'.",
      key_concepts: ["this", "function call vs method call"],
    },
    {
      id: "js_this_keyword_wc3",
      type: "fix",
      prompt: "Fix this code so `this` correctly refers to the `counter` object inside the arrow function, by not using an arrow function for the method itself.",
      broken_code: "const counter = {\n  count: 0,\n  increment: () => {\n    this.count += 1;\n    return this.count;\n  },\n};\nconsole.log(counter.increment());",
      solution_code: "const counter = {\n  count: 0,\n  increment() {\n    this.count += 1;\n    return this.count;\n  },\n};\nconsole.log(counter.increment());",
      expected_output: "1",
      hints: ["Arrow functions don't have their own `this` — they inherit it from their surrounding scope, not from the object they're attached to.", "Using a regular method (shorthand syntax) makes `this` correctly refer to counter when called as counter.increment()."],
      solution_summary: "Switching to a regular method makes `this` correctly refer to counter when called as counter.increment(), so count increments to 1 as expected.",
      key_concepts: ["this", "arrow functions"],
    },
    {
      id: "js_this_keyword_wc4",
      type: "fix",
      prompt: "Fix this code so calling `getName` standalone (detached from its object) still works correctly, using .bind().",
      broken_code: "const user = {\n  name: \"Robin\",\n  getName() {\n    return this.name;\n  },\n};\nconst getName = user.getName;\nconsole.log(getName());",
      solution_code: "const user = {\n  name: \"Robin\",\n  getName() {\n    return this.name;\n  },\n};\nconst getName = user.getName.bind(user);\nconsole.log(getName());",
      expected_output: "Robin",
      hints: [".bind(user) permanently locks `this` to user, regardless of how the resulting function is later called.", "Without .bind, calling getName() standalone loses its connection to user."],
      solution_summary: ".bind(user) locks this to user permanently, so calling the bound getName() standalone still correctly returns 'Robin'.",
      key_concepts: ["this", ".bind()"],
    },
    {
      id: "js_this_keyword_wc5",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "const car = {\n  brand: \"Volt\",\n  describe() {\n    return `This is a ${this.brand}`;\n  },\n};\nconsole.log(car.describe());",
      expected_output: "This is a Volt",
      hints: ["describe() is called as car.describe(), so `this` refers to car."],
      solution_summary: "Since describe() is called as car.describe(), this.brand is 'Volt', printing 'This is a Volt'.",
      key_concepts: ["this", "method call"],
    },
    {
      id: "js_this_keyword_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "const obj = {\n  value: 10,\n  getValue: () => typeof this?.value,\n};\nconsole.log(obj.getValue());",
      expected_output: "undefined",
      hints: ["Arrow functions don't bind their own `this` — they use whatever `this` was in the surrounding scope where they were defined, not obj.", "At the top level here, `this` doesn't have a `value` property, so this?.value is undefined, and typeof that is the string 'undefined'."],
      solution_summary: "Because getValue is an arrow function, `this` isn't bound to obj, so this?.value is undefined, and typeof that result is the string 'undefined'.",
      key_concepts: ["this", "arrow functions"],
    },
    {
      id: "js_this_keyword_wc7",
      type: "build",
      prompt: "Write an object `dog` with a `name` property and a regular method `bark()` that returns '<name> says woof', then log `dog.bark()` for name \"Rex\".",
      solution_code: "const dog = {\n  name: \"Rex\",\n  bark() {\n    return `${this.name} says woof`;\n  },\n};\nconsole.log(dog.bark());",
      expected_output: "Rex says woof",
      hints: ["Use `this.name` inside bark() to access the object's own name property.", "bark() must be called as dog.bark() so `this` refers to dog."],
      solution_summary: "Calling dog.bark() makes `this` refer to dog, so this.name is 'Rex', printing 'Rex says woof'.",
      key_concepts: ["this", "method call"],
    },
    {
      id: "js_this_keyword_wc8",
      type: "build",
      prompt: "Write an object `account` with a `balance` property and a method `deposit(amount)` that adds amount to this.balance and returns the new balance, then log the result of depositing 50 into a balance starting at 100.",
      solution_code: "const account = {\n  balance: 100,\n  deposit(amount) {\n    this.balance += amount;\n    return this.balance;\n  },\n};\nconsole.log(account.deposit(50));",
      expected_output: "150",
      hints: ["Use this.balance to access and update the object's own balance property.", "deposit() must be called as account.deposit() so `this` refers to account."],
      solution_summary: "account.deposit(50) adds 50 to this.balance (100), returning and printing 150.",
      key_concepts: ["this", "method call"],
    },
  ],
  js_frontend_vs_backend_node: [
    {
      id: "js_frontend_vs_backend_node_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating why frontend JavaScript runs in a restricted sandbox.",
      shuffled_lines: [
        "console.log(\"Frontend JavaScript runs inside a browser's deliberately restricted sandbox.\");",
        "console.log(\"It has access to the DOM and various browser-provided APIs.\");",
        "console.log(\"But it has no direct access to a user's own file system, for real security reasons.\");",
      ],
      solution_code: "console.log(\"Frontend JavaScript runs inside a browser's deliberately restricted sandbox.\");\nconsole.log(\"It has access to the DOM and various browser-provided APIs.\");\nconsole.log(\"But it has no direct access to a user's own file system, for real security reasons.\");",
      expected_output: "Frontend JavaScript runs inside a browser's deliberately restricted sandbox.\nIt has access to the DOM and various browser-provided APIs.\nBut it has no direct access to a user's own file system, for real security reasons.",
      hints: ["The sandbox is described first, then what it does allow, then what it restricts.", "File system access is the specific thing explicitly denied."],
      solution_summary: "Frontend JavaScript runs in a restricted sandbox, with DOM/browser API access allowed but no direct file system access, for security.",
      key_concepts: ["frontend JavaScript", "sandbox"],
    },
    {
      id: "js_frontend_vs_backend_node_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating what Node.js enables that the browser doesn't.",
      shuffled_lines: [
        "console.log(\"Node.js runs JavaScript outside the browser, directly on a server.\");",
        "console.log(\"Without the browser's sandbox, Node.js code can access the file system directly.\");",
        "console.log(\"This lets JavaScript be used for backend tasks like reading and writing files.\");",
      ],
      solution_code: "console.log(\"Node.js runs JavaScript outside the browser, directly on a server.\");\nconsole.log(\"Without the browser's sandbox, Node.js code can access the file system directly.\");\nconsole.log(\"This lets JavaScript be used for backend tasks like reading and writing files.\");",
      expected_output: "Node.js runs JavaScript outside the browser, directly on a server.\nWithout the browser's sandbox, Node.js code can access the file system directly.\nThis lets JavaScript be used for backend tasks like reading and writing files.",
      hints: ["Node.js's context (server, not browser) is described first.", "The specific backend capability (file access) is the final, concrete consequence."],
      solution_summary: "Node.js runs JavaScript on a server outside the browser sandbox, enabling direct file system access and backend tasks.",
      key_concepts: ["Node.js", "backend JavaScript"],
    },
    {
      id: "js_frontend_vs_backend_node_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what frontend JavaScript can access.",
      broken_code: "console.log(\"Frontend JavaScript running in a browser has full direct access to the user's file system.\");",
      solution_code: "console.log(\"Frontend JavaScript running in a browser has no direct access to the user's file system.\");",
      expected_output: "Frontend JavaScript running in a browser has no direct access to the user's file system.",
      hints: ["The browser sandbox specifically denies direct file system access, for security reasons.", "This is one of the defining restrictions of frontend JavaScript."],
      solution_summary: "The corrected statement accurately describes frontend JavaScript as having no direct file system access, unlike the original incorrect claim.",
      key_concepts: ["frontend JavaScript", "sandbox"],
    },
    {
      id: "js_frontend_vs_backend_node_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what Node.js is.",
      broken_code: "console.log(\"Node.js is a separate programming language, unrelated to JavaScript.\");",
      solution_code: "console.log(\"Node.js is a runtime that lets JavaScript run outside the browser, on a server.\");",
      expected_output: "Node.js is a runtime that lets JavaScript run outside the browser, on a server.",
      hints: ["Node.js runs the same JavaScript language, just in a different environment.", "It's a runtime environment, not a separate language."],
      solution_summary: "The corrected statement accurately describes Node.js as a runtime for running JavaScript outside the browser, not a separate language.",
      key_concepts: ["Node.js"],
    },
    {
      id: "js_frontend_vs_backend_node_wc5",
      type: "output",
      prompt: "What does this code print, simulating listing what frontend versus backend JavaScript can access?",
      solution_code: "const access = { frontend: \"DOM and browser APIs\", backend: \"file system and databases\" };\nfor (const [context, capability] of Object.entries(access)) {\n  console.log(`${context}: ${capability}`);\n}",
      expected_output: "frontend: DOM and browser APIs\nbackend: file system and databases",
      hints: ["Object.entries iterates the access object's key/value pairs in insertion order."],
      solution_summary: "Object.entries iterates the access object in order, printing each context alongside its capability.",
      key_concepts: ["frontend JavaScript", "Node.js"],
    },
    {
      id: "js_frontend_vs_backend_node_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function canAccessFileSystem(environment) {\n  return environment === \"node\";\n}\n\nconsole.log(canAccessFileSystem(\"browser\"));",
      expected_output: "false",
      hints: ["\"browser\" !== \"node\", so the strict equality check returns false."],
      solution_summary: "environment is \"browser\", not \"node\", so canAccessFileSystem returns false.",
      key_concepts: ["frontend JavaScript", "Node.js"],
    },
    {
      id: "js_frontend_vs_backend_node_wc7",
      type: "build",
      prompt: "Write a function `describeEnvironment(env)` that returns 'sandboxed browser environment' for \"browser\" and 'unrestricted server environment' for anything else, then log the result for \"node\".",
      solution_code: "function describeEnvironment(env) {\n  if (env === \"browser\") {\n    return \"sandboxed browser environment\";\n  }\n  return \"unrestricted server environment\";\n}\n\nconsole.log(describeEnvironment(\"node\"));",
      expected_output: "unrestricted server environment",
      hints: ["Check specifically whether env equals \"browser\" first."],
      solution_summary: "env is \"node\", not \"browser\", so the function falls through to return 'unrestricted server environment'.",
      key_concepts: ["frontend JavaScript", "Node.js"],
    },
    {
      id: "js_frontend_vs_backend_node_wc8",
      type: "build",
      prompt: "Write code storing frontend and backend capabilities in an object and log each formatted as '<context> can access: <capability>'.",
      solution_code: "const capabilities = { frontend: \"DOM\", backend: \"file system\" };\nfor (const [context, capability] of Object.entries(capabilities)) {\n  console.log(`${context} can access: ${capability}`);\n}",
      expected_output: "frontend can access: DOM\nbackend can access: file system",
      hints: ["Iterate Object.entries(capabilities) to access both the context and its capability together."],
      solution_summary: "Object.entries iterates capabilities in order, printing each context alongside what it can access.",
      key_concepts: ["frontend JavaScript", "Node.js"],
    },
  ],
  js_typescript_intro: [
    {
      id: "js_typescript_intro_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating how TypeScript differs from Python's type hints.",
      shuffled_lines: [
        "console.log(\"TypeScript adds static typing on top of plain JavaScript.\");",
        "console.log(\"This is conceptually similar to Python's type hints.\");",
        "console.log(\"But TypeScript's annotations are actually enforced at compile time, before code ever runs.\");",
      ],
      solution_code: "console.log(\"TypeScript adds static typing on top of plain JavaScript.\");\nconsole.log(\"This is conceptually similar to Python's type hints.\");\nconsole.log(\"But TypeScript's annotations are actually enforced at compile time, before code ever runs.\");",
      expected_output: "TypeScript adds static typing on top of plain JavaScript.\nThis is conceptually similar to Python's type hints.\nBut TypeScript's annotations are actually enforced at compile time, before code ever runs.",
      hints: ["The basic description of TypeScript comes first, then the comparison to Python.", "The key contrast (enforced at compile time) is the final, most important distinction."],
      solution_summary: "TypeScript adds static typing similar to Python's type hints, but its annotations are actually enforced at compile time, unlike Python's optional hints.",
      key_concepts: ["TypeScript", "static typing"],
    },
    {
      id: "js_typescript_intro_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating how the TypeScript compiler catches an error before code ever runs.",
      shuffled_lines: [
        "console.log(\"A developer writes a function expecting a number parameter.\");",
        "console.log(\"They accidentally call it with a string argument instead.\");",
        "console.log(\"The TypeScript compiler flags this mismatch before the code is ever allowed to run.\");",
      ],
      solution_code: "console.log(\"A developer writes a function expecting a number parameter.\");\nconsole.log(\"They accidentally call it with a string argument instead.\");\nconsole.log(\"The TypeScript compiler flags this mismatch before the code is ever allowed to run.\");",
      expected_output: "A developer writes a function expecting a number parameter.\nThey accidentally call it with a string argument instead.\nThe TypeScript compiler flags this mismatch before the code is ever allowed to run.",
      hints: ["The function's expectation is described before the mismatched call that violates it.", "The compiler's error-catching happens as the final consequence, before runtime."],
      solution_summary: "A function expects a number, is mistakenly called with a string, and the TypeScript compiler flags that mismatch before the code ever runs.",
      key_concepts: ["TypeScript", "compile-time checking"],
    },
    {
      id: "js_typescript_intro_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes when TypeScript's type errors are caught.",
      broken_code: "console.log(\"TypeScript type errors are only ever discovered after the program has already crashed in production.\");",
      solution_code: "console.log(\"TypeScript type errors are caught by the compiler before the program is ever allowed to run.\");",
      expected_output: "TypeScript type errors are caught by the compiler before the program is ever allowed to run.",
      hints: ["TypeScript's whole value proposition is catching errors before runtime, not after a production crash.", "The compiler enforces types before code execution begins."],
      solution_summary: "The corrected statement accurately describes TypeScript catching type errors at compile time, before the program ever runs, not after a production crash.",
      key_concepts: ["TypeScript", "compile-time checking"],
    },
    {
      id: "js_typescript_intro_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes TypeScript's relationship to plain JavaScript.",
      broken_code: "console.log(\"TypeScript is a completely separate language that shares no relationship with JavaScript at all.\");",
      solution_code: "console.log(\"TypeScript is JavaScript with static type annotations added on top, compiling down to plain JavaScript.\");",
      expected_output: "TypeScript is JavaScript with static type annotations added on top, compiling down to plain JavaScript.",
      hints: ["TypeScript code ultimately compiles down to regular JavaScript to actually run.", "It's an extension of JavaScript, not an unrelated separate language."],
      solution_summary: "The corrected statement accurately describes TypeScript as JavaScript plus static types, compiling down to plain JavaScript, not an unrelated language.",
      key_concepts: ["TypeScript", "JavaScript"],
    },
    {
      id: "js_typescript_intro_wc5",
      type: "output",
      prompt: "What does this code print, simulating type-checking facts as data?",
      solution_code: "const facts = { checked: \"compile time\", enforced: true, language: \"superset of JavaScript\" };\nfor (const [key, value] of Object.entries(facts)) {\n  console.log(`${key}: ${value}`);\n}",
      expected_output: "checked: compile time\nenforced: true\nlanguage: superset of JavaScript",
      hints: ["Object.entries iterates the facts object's key/value pairs in insertion order."],
      solution_summary: "Object.entries iterates the facts object in order, printing each key alongside its value.",
      key_concepts: ["TypeScript", "compile-time checking"],
    },
    {
      id: "js_typescript_intro_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function wouldTypeScriptFlag(expectedType, actualType) {\n  return expectedType !== actualType;\n}\n\nconsole.log(wouldTypeScriptFlag(\"number\", \"string\"));",
      expected_output: "true",
      hints: ["\"number\" !== \"string\" is true, meaning TypeScript would flag this mismatch."],
      solution_summary: "Since \"number\" !== \"string\" is true, wouldTypeScriptFlag returns true, meaning TypeScript would catch this type mismatch.",
      key_concepts: ["TypeScript", "type checking"],
    },
    {
      id: "js_typescript_intro_wc7",
      type: "build",
      prompt: "Write a function `describeTypeSystem(enforced)` that returns 'compiler enforced' if enforced is true, otherwise 'developer optional', then log the result for true.",
      solution_code: "function describeTypeSystem(enforced) {\n  return enforced ? \"compiler enforced\" : \"developer optional\";\n}\n\nconsole.log(describeTypeSystem(true));",
      expected_output: "compiler enforced",
      hints: ["Use a ternary expression checking the enforced boolean directly."],
      solution_summary: "Since enforced is true, the ternary evaluates to 'compiler enforced'.",
      key_concepts: ["TypeScript", "compile-time checking"],
    },
    {
      id: "js_typescript_intro_wc8",
      type: "build",
      prompt: "Write code storing facts about TypeScript versus plain JavaScript typing in an object and log each formatted as '<language>: <typing style>'.",
      solution_code: "const typing = { JavaScript: \"dynamically typed at runtime\", TypeScript: \"statically typed at compile time\" };\nfor (const [language, style] of Object.entries(typing)) {\n  console.log(`${language}: ${style}`);\n}",
      expected_output: "JavaScript: dynamically typed at runtime\nTypeScript: statically typed at compile time",
      hints: ["Object.entries gives you [language, style] pairs to destructure directly in the loop."],
      solution_summary: "Object.entries iterates the typing object in order, printing each language alongside its typing style.",
      key_concepts: ["TypeScript", "static typing", "dynamic typing"],
    },
  ],
  js_common_libraries: [
    {
      id: "js_common_libraries_wc1",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating why widely used JavaScript libraries beyond React and Vue are rarely worth reinventing.",
      shuffled_lines: [
        "console.log(\"Beyond React and Vue, several especially widely used JavaScript libraries solve common problems well.\");",
        "console.log(\"A developer needs to format dates, make HTTP calls, or manipulate arrays reliably.\");",
        "console.log(\"Reinventing these from scratch for a given project is rarely worthwhile.\");",
      ],
      solution_code: "console.log(\"Beyond React and Vue, several especially widely used JavaScript libraries solve common problems well.\");\nconsole.log(\"A developer needs to format dates, make HTTP calls, or manipulate arrays reliably.\");\nconsole.log(\"Reinventing these from scratch for a given project is rarely worthwhile.\");",
      expected_output: "Beyond React and Vue, several especially widely used JavaScript libraries solve common problems well.\nA developer needs to format dates, make HTTP calls, or manipulate arrays reliably.\nReinventing these from scratch for a given project is rarely worthwhile.",
      hints: ["The general claim about common libraries comes first, followed by specific needs they solve.", "The conclusion about not reinventing them is the final takeaway."],
      solution_summary: "Common JavaScript libraries solve well-understood problems like dates, HTTP calls, and array manipulation, making them rarely worth reinventing.",
      key_concepts: ["common libraries", "JavaScript ecosystem"],
    },
    {
      id: "js_common_libraries_wc2",
      type: "reorder",
      prompt: "Reorder these lines into a program narrating what a few specific common libraries are used for.",
      shuffled_lines: [
        "console.log(\"axios is widely used for making HTTP requests from JavaScript.\");",
        "console.log(\"dayjs is widely used for parsing and formatting dates.\");",
        "console.log(\"lodash is widely used for utility functions on arrays and objects.\");",
      ],
      solution_code: "console.log(\"axios is widely used for making HTTP requests from JavaScript.\");\nconsole.log(\"dayjs is widely used for parsing and formatting dates.\");\nconsole.log(\"lodash is widely used for utility functions on arrays and objects.\");",
      expected_output: "axios is widely used for making HTTP requests from JavaScript.\ndayjs is widely used for parsing and formatting dates.\nlodash is widely used for utility functions on arrays and objects.",
      hints: ["Each line names one specific library and its specific common use case."],
      solution_summary: "axios handles HTTP requests, dayjs handles dates, and lodash provides array/object utility functions — three distinct common libraries and their purposes.",
      key_concepts: ["axios", "dayjs", "lodash"],
    },
    {
      id: "js_common_libraries_wc3",
      type: "fix",
      prompt: "Fix this code so it accurately describes what the lodash library is used for.",
      broken_code: "console.log(\"lodash is specifically a library for rendering user interface components.\");",
      solution_code: "console.log(\"lodash is a library of utility functions for working with arrays, objects, and other common data structures.\");",
      expected_output: "lodash is a library of utility functions for working with arrays, objects, and other common data structures.",
      hints: ["lodash provides general-purpose utility functions, not UI rendering.", "React and Vue, not lodash, are the libraries for rendering UI components."],
      solution_summary: "The corrected statement accurately describes lodash as a utility library for arrays/objects, not a UI rendering library.",
      key_concepts: ["lodash"],
    },
    {
      id: "js_common_libraries_wc4",
      type: "fix",
      prompt: "Fix this code so it accurately describes what axios is used for.",
      broken_code: "console.log(\"axios is a library specifically for formatting and parsing calendar dates.\");",
      solution_code: "console.log(\"axios is a library for making HTTP requests, such as GET and POST calls, from JavaScript.\");",
      expected_output: "axios is a library for making HTTP requests, such as GET and POST calls, from JavaScript.",
      hints: ["axios is specifically an HTTP request library, not a date-handling library.", "dayjs, not axios, is the library commonly used for dates."],
      solution_summary: "The corrected statement accurately describes axios as an HTTP request library, not a date-formatting library.",
      key_concepts: ["axios"],
    },
    {
      id: "js_common_libraries_wc5",
      type: "output",
      prompt: "What does this code print, simulating a list of common libraries and their purposes?",
      solution_code: "const libraries = { axios: \"HTTP requests\", dayjs: \"date handling\", lodash: \"utility functions\" };\nfor (const [name, purpose] of Object.entries(libraries)) {\n  console.log(`${name}: ${purpose}`);\n}",
      expected_output: "axios: HTTP requests\ndayjs: date handling\nlodash: utility functions",
      hints: ["Object.entries iterates the libraries object's key/value pairs in insertion order."],
      solution_summary: "Object.entries iterates the libraries object in order, printing each library name alongside its purpose.",
      key_concepts: ["axios", "dayjs", "lodash"],
    },
    {
      id: "js_common_libraries_wc6",
      type: "output",
      prompt: "What does this code print?",
      solution_code: "function recommendLibrary(task) {\n  const map = { dates: \"dayjs\", http: \"axios\", utilities: \"lodash\" };\n  return map[task];\n}\n\nconsole.log(recommendLibrary(\"http\"));",
      expected_output: "axios",
      hints: ["map['http'] looks up the 'http' key directly in the map object."],
      solution_summary: "recommendLibrary('http') looks up 'http' in the map object and returns 'axios'.",
      key_concepts: ["axios", "common libraries"],
    },
    {
      id: "js_common_libraries_wc7",
      type: "build",
      prompt: "Write code storing three common libraries and their purposes in an object, then log each formatted as '<name> handles <purpose>'.",
      solution_code: "const libraries = { axios: \"HTTP requests\", dayjs: \"dates\", lodash: \"array/object utilities\" };\nfor (const [name, purpose] of Object.entries(libraries)) {\n  console.log(`${name} handles ${purpose}`);\n}",
      expected_output: "axios handles HTTP requests\ndayjs handles dates\nlodash handles array/object utilities",
      hints: ["Iterate Object.entries(libraries) to access both the library name and its purpose."],
      solution_summary: "Object.entries iterates libraries in order, printing each name alongside what it handles.",
      key_concepts: ["axios", "dayjs", "lodash"],
    },
    {
      id: "js_common_libraries_wc8",
      type: "build",
      prompt: "Write a function `recommendLibrary(task)` that returns 'dayjs' for \"dates\" and 'axios' for anything else, then log the result for \"dates\".",
      solution_code: "function recommendLibrary(task) {\n  if (task === \"dates\") {\n    return \"dayjs\";\n  }\n  return \"axios\";\n}\n\nconsole.log(recommendLibrary(\"dates\"));",
      expected_output: "dayjs",
      hints: ["Check specifically whether task equals \"dates\" first."],
      solution_summary: "task is \"dates\", so the function returns 'dayjs' via the first branch.",
      key_concepts: ["dayjs", "common libraries"],
    },
  ],
};

let added = 0;
for (const [topicId, challenges] of Object.entries(CONTENT)) {
  const topic = jsTrack.topics.find((t) => t.id === topicId);
  if (!topic) {
    console.error(`MISSING TOPIC: ${topicId}`);
    continue;
  }
  if (!Array.isArray(topic.anvil_challenges)) topic.anvil_challenges = [];
  topic.anvil_challenges.push(...challenges);
  added += challenges.length;
}

fs.writeFileSync(KB_PATH, JSON.stringify(kb, null, 2) + "\n", "utf8");
console.log(`Added ${added} challenges across ${Object.keys(CONTENT).length} topics.`);
